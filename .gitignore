build/
bin/
*.ini

# Auto-generated by cmake/GenVersion.cmake — bootstrap with:
#   cmake -S . -B build && cmake --build build --target av_gen_version
doc_typst/_version.typ

# Offline sync artifacts (macOS → Linux LAN transfer) — generated by script/sync-linux.sh
VERSION.txt
VERSION_DATE.txt

# Large prebuilt bundles / vendored deps / test data — kept out of git (regenerated/fetched)
av-appimage-tools.tar.gz
av-deps.tar.gz
/deps/
/test/
/tigervnc-debs/

# Local machine-specific Claude settings
.claude/settings.local.json
.DS_Store

# Windows 시스템 설치 산출물 (win-install.sh가 원격에서 생성)
install-sys.cmd
install-sys.log
