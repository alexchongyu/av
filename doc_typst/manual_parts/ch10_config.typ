#import "_common.typ": *

= 설정 영속화와 문제 해결

av 는 종료할 때 화면 상태의 일부를 홈 디렉토리의 텍스트 파일에 적어 두고, 다음
실행 때 그대로 복원합니다. 어떤 항목이 저장되고 어떤 항목은 매번 초기화되는지
정확히 알아 두면 "어제와 화면이 다르다"는 혼란을 없앨 수 있습니다. 이 장에서는
설정 파일의 위치와 저장되는 키 전수, 설정을 되돌리는 방법, 모드에 따라 의미가
바뀌는 키의 대조표, 그리고 현장에서 자주 부딪히는 증상별 해결책을 다룹니다.

== 설정 파일 `.av.ini`

=== 위치와 이름

설정 파일 이름은 점으로 시작하는 `.av.ini` 입니다 (`av.ini` 가 아닙니다). 경로는
환경변수 `HOME` 하나만으로 결정되며, ICC 프로파일이나 XDG 규격 디렉토리는
사용하지 않습니다.

#table(
  columns: (auto, 1fr),
  [*환경*], [*실제 경로*],
  [macOS], [`/Users/<사용자>/.av.ini`],
  [Linux], [`/home/<사용자>/.av.ini`],
  [Windows \u{2014} MSYS2 / Git Bash / WSL], [`$HOME/.av.ini` (셸이 `HOME` 을 설정함)],
  [Windows \u{2014} cmd.exe / PowerShell], [`HOME` 이 없으면 *현재 작업 디렉토리*의 `.av.ini`],
)

#warn[
Windows 의 `cmd.exe` 나 PowerShell 에는 보통 `HOME` 이 정의돼 있지 않습니다. 이때
av 는 경로를 `.` 로 대체하므로, 설정이 *av 를 실행한 폴더*에 남습니다. 폴더를
바꿔 실행하면 설정이 초기화된 것처럼 보입니다. 항상 같은 설정을 쓰려면 실행 전에
`setx HOME %USERPROFILE%` 로 `HOME` 을 한 번 정의해 두십시오.
]

=== 저장 시점과 읽는 시점

#table(
  columns: (auto, 1fr),
  [*시점*], [*동작*],
  [프로그램 시작], [`.av.ini` 를 읽어 상태에 반영 (파일이 없으면 조용히 무시)],
  [곧이어 CLI 적용], [CLI 옵션이 같은 항목을 덮어씀 \u{2014} *CLI 가 항상 이깁니다*],
  [실행 중], [저장하지 않음. 키를 눌러 토글해도 파일은 그대로],
  [정상 종료], [#key("Q") 또는 창 닫기 \u{2192} 이벤트 루프 종료 후 파일 *전체를 새로 씀*],
  [비정상 종료], [`kill -9`, 크래시, 정전 \u{2192} *저장되지 않음*],
)

즉 저장은 "변경 즉시"가 아니라 "정상 종료 시 1회"입니다. 실행 중에 손으로 파일을
편집해도 종료할 때 전부 덮어써지므로, 파일을 직접 고칠 때는 반드시 av 를 먼저
종료하십시오.

#note[
av 를 여러 개 띄우고 각각 다른 상태로 종료하면 *마지막에 종료한 인스턴스*의 상태만
남습니다. 한쪽에서는 히스토그램을 열어 두고 다른 쪽에서는 닫은 채 끄면, 나중에 끈
쪽이 이깁니다.
]

=== 파일 형식

`key=value` 한 줄에 한 항목, `#` 으로 시작하는 줄은 주석, `=` 가 없는 줄은 무시하는
아주 단순한 형식입니다. 모르는 키도 그냥 건너뜁니다. 실제로 저장된 파일은 다음과
같은 모습입니다.

```
# av configuration
show_borders=1
pixel_format=0
magnifier_active=1
show_crosshair=0
sync_viewports=1
show_ui=0
show_info=0
show_pixel_info=0
show_histogram=0
show_hline_cut=0
show_vline_cut=0
show_stats=0
show_hotkey_help=0
show_scatter_plot=0
histogram_compare=0
channel_mode=0
windowed_mode=0
overlay_active=0
zoom=0
```

불리언 값은 "`0` 이면 거짓, 그 외 문자열은 모두 참"으로 해석합니다. `show_ui=true`
라고 써도 참으로 동작합니다.

=== 저장되는 키 전수

파일에 기록되는 키는 정확히 19개이며, 위 예시의 순서 그대로 매번 새로 쓰입니다.

#table(
  columns: (auto, auto, auto, 1fr),
  [*키*], [*값*], [*단축키*], [*의미 (기본값)*],
  [`show_borders`],     [`0`/`1`], [#key("B")],            [패널 테두리 표시 (기본 `1`)],
  [`show_ui`],          [`0`/`1`], [#key("U")],            [메뉴바 + 상태바 오버레이 (기본 `0`)],
  [`windowed_mode`],    [`0`/`1`], [#key("W")],            [타이틀바 있는 창 모드 (기본 `0`)],
  [`show_crosshair`],   [`0`/`1`], [#key("M")],            [십자선 오버레이 (기본 `0`)],
  [`magnifier_active`], [`0`/`1`], [#key("Ctrl+M")],       [돋보기 (diff 아닌 모드, 기본 `1`)],
  [`overlay_active`],   [`0`/`1`], [#key("O")],            [Overlay/Blend 비교 모드 (기본 `0`)],
  [`sync_viewports`],   [`0`/`1`], [#key("S")],            [A/B 뷰포트 동기 (기본 `1`)],
)

#table(
  columns: (auto, auto, auto, 1fr),
  [*키*], [*값*], [*단축키*], [*의미 (기본값)*],
  [`show_info`],        [`0`/`1`], [#key("I")],            [Image Info 창 (기본 `0`)],
  [`show_pixel_info`],  [`0`/`1`], [#key("V")],            [커서 픽셀 값 풍선 (기본 `0`)],
  [`show_histogram`],   [`0`/`1`], [#key("Ctrl+H")],       [히스토그램 창 (기본 `0`)],
  [`show_hline_cut`],   [`0`/`1`], [#key("Ctrl+L")],       [수평 라인 컷 창 (기본 `0`)],
  [`show_vline_cut`],   [`0`/`1`], [#key("Ctrl+Y")],       [수직 라인 컷 창 (기본 `0`)],
  [`show_stats`],       [`0`/`1`], [#key("Ctrl+S")],       [Image Statistics 창 (기본 `0`)],
  [`show_scatter_plot`],[`0`/`1`], [#key("Ctrl+T")],       [Scatter Plot 창 (기본 `0`)],
  [`show_hotkey_help`], [`0`/`1`], [#key("Ctrl+Shift+H")], [핫키 레퍼런스 창 (기본 `0`)],
  [`histogram_compare`],[`0`/`1`], [\u{2014} (창 안 체크박스)], [히스토그램 A/B 겹쳐 보기 (기본 `0`)],
)

#table(
  columns: (auto, auto, auto, 1fr),
  [*키*], [*값*], [*단축키*], [*의미 (기본값)*],
  [`channel_mode`], [`0`\~`4`], [#key("Shift+C") 등],
    [채널 표시: `0`=RGB(#key("Shift+C")), `1`=R(#key("Shift+R")), `2`=G(#key("Shift+G")), `3`=B(#key("Shift+B")), `4`=Luma Y'(#key("Shift+Y")) (기본 `0`)],
  [`pixel_format`], [`0`\~`2`], [#key("Ctrl+X")],
    [픽셀 값 표기: `0`=10진, `1`=`0x80`, `2`=`80h` (기본 `0`)],
  [`zoom`], [`0` 또는 배율], [`--zoom`],
    [초기 줌: `0`=fit, `>0`=고정 배율 (기본 `0`). 적용 시 `0.125`\~`256` 으로 클램프],
)

#warn[
`zoom` 은 "지금 화면의 배율"이 아니라 *초기 배율 설정*입니다. #key("Z") 나 휠로 확대해도
이 값은 바뀌지 않습니다. 값이 바뀌는 경로는 `--zoom` 을 명시했을 때뿐이며, 한 번
`av --zoom 4 img.png` 로 띄우면 `zoom=4` 가 저장되어 그 다음부터는 인자 없이 실행해도
400% 로 시작합니다. 되돌리려면 `av --zoom fit img.png` 로 한 번 띄웠다 끄거나
`.av.ini` 의 `zoom=` 줄을 지우십시오.
]

=== 저장은 되지만 다음 실행에 반영되지 않는 두 키

`sync_viewports` 와 `windowed_mode` 는 파일에 기록되지만, 시작 시 CLI 옵션 적용
단계에서 *무조건* CLI 기본값으로 덮어써집니다. 따라서 실질적으로는 매번
초기화됩니다.

#table(
  columns: (auto, auto, 1fr),
  [*키*], [*시작 시 실제 값*], [*원하는 상태로 시작하는 방법*],
  [`sync_viewports`], [`--sync` 기본값 = 켜짐], [끄고 시작하려면 `--no-sync` 를 붙입니다],
  [`windowed_mode`],  [`--windowed` 기본값 = 꺼짐], [창 모드로 시작하려면 `--windowed` 를 붙입니다],
)

#tip[
자주 쓰는 조합은 셸 alias 로 굳혀 두는 편이 확실합니다.

```
alias avw='av --windowed --geometry 1600x900'
alias avn='av --no-sync'
```
]

=== 영속화되지 않는 상태

아래 항목은 파일에 기록되지 않으므로 실행할 때마다 기본값에서 출발합니다. 검증
조건을 재현하려면 CLI 옵션으로 지정하거나 스크립트로 감싸야 합니다.

#table(
  columns: (auto, 1fr),
  [*항목*], [*시작값 / 지정 방법*],
  [Diff 모드], [항상 `none`. `--diff-mode` 또는 `-d` 로 지정 (4장 참조)],
  [Diff amplify / threshold], [`1.0` / `0`. `--amplify` 로 지정],
  [`--comp` 관련 전부], [`--comp` `--blk` `--num_blk` `--blk-metric` `--grid` 로 매번 지정 (5장 참조)],
  [worst 박스, 그리드, 히트맵, 승패맵], [`show_worst` 만 켜짐으로 시작, 나머지는 꺼짐],
  [LUT / CDL 적용], [`--lut` / `--cdl` 로 로드해야 켜짐 (9장 참조)],
  [불량 픽셀 오버레이 #key("/")], [항상 꺼짐],
  [콜러메트리 표시 #key("Shift+V")], [항상 꺼짐],
  [ROI 선택 #key("Ctrl+E")], [항상 꺼짐],
  [블링크 간격 / 슬라이드쇼 간격], [`0.5`초 / `3.0`초 고정],
  [pan step], [`32`px. `-p` 또는 `--pan-step` 으로 지정],
  [패널 테두리 색], [magenta/yellow/cyan. `-bc` 로 지정],
  [Pathfinder 미니맵], [항상 이미지 미니맵 모드(`1`)로 시작],
  [활성 패널], [항상 A],
)

=== 창 배치 파일 `.av_imgui.ini`

히스토그램·통계·핫키 레퍼런스 같은 도구 창의 *위치와 크기*는 `.av.ini` 가 아니라
ImGui 가 관리하는 별도 파일 `.av_imgui.ini` 에 저장됩니다. 이 파일 이름은 상대
경로이므로 *av 를 실행한 작업 디렉토리*에 생깁니다. 창 배치가 화면 밖으로 밀려나
보이지 않는다면 이 파일을 지우면 됩니다.

```
rm .av_imgui.ini      # 창 배치만 초기화 (토글 상태는 유지)
```

== 설정 초기화와 부분 되돌리기

=== 전체 초기화

가장 확실한 방법은 파일을 지우는 것입니다. av 를 종료한 상태에서 실행하십시오.

```
rm ~/.av.ini
rm .av_imgui.ini
```

다음 실행에서 av 는 소스에 하드코딩된 기본값(위 표의 "기본값" 열)으로 시작하고,
종료할 때 파일을 다시 만듭니다.

=== 특정 설정만 되돌리기

로더는 *파일에 있는 키만* 상태에 반영합니다. 따라서 되돌리고 싶은 항목의 줄만
지우면 그 항목만 기본값으로 돌아갑니다.

#ex("히스토그램 창만 안 뜨게 되돌리기")[
1. av 를 종료합니다.
2. `~/.av.ini` 에서 `show_histogram=1` 줄을 삭제하거나 `show_histogram=0` 으로 고칩니다.
3. av 를 다시 띄우면 히스토그램 창이 닫힌 상태로 시작합니다.

같은 작업을 셸 한 줄로 하려면:

```
sed -i.bak '/^show_histogram=/d' ~/.av.ini
```
]

#warn[
숫자 키(`pixel_format`, `channel_mode`, `zoom`)에 숫자가 아닌 값이나 빈 값을 넣으면
파싱 예외가 발생해 av 가 시작 즉시 죽습니다. `zoom=` 처럼 값을 비워 두지 말고,
지울 때는 줄 전체를 지우십시오. 불리언 키는 어떤 문자열이 들어와도 안전합니다.
]

=== 설정을 고정해서 잠그는 방법

여러 사람이 쓰는 검증 장비나 CI 이미지에서는 "누가 뭘 눌러도 다음 사람에게 같은
화면"이 필요합니다. 저장 루틴은 파일을 열지 못하면 조용히 넘어가므로, 원하는
상태로 한 번 저장한 뒤 파일을 읽기 전용으로 만들면 그대로 굳습니다.

```
av golden.png hw.png      # 원하는 상태로 맞춘 뒤 Q 로 종료
chmod 444 ~/.av.ini       # 이후 종료 시 저장 시도는 무시됨
```

== 모드별 키 재정의 대조표

가장 헷갈리는 부분입니다. `--comp` (3영상 블록 비교) 모드에서는 같은 키가 다른
일을 합니다. 아래 표의 키들은 *모드에 따라 의미가 완전히 바뀌므로* 반드시
구분해서 기억하십시오.

#table(
  columns: (auto, 1fr, 1fr),
  [*키*], [*일반 모드 (A/B, diff)*], [*`--comp` 모드 (3영상)*],
  [#key("S")],           [뷰포트 동기 on/off 토글], [img2 \u{2194} img3 패널 위치 교환 (통계 재계산)],
  [#key("G")],           [뷰를 이미지 중앙으로 리셋], [블록 그리드 경계선 표시 토글],
  [#key("Ctrl+G")],      [동작 없음], [블록 PSNR 히트맵 토글 (노랑\u{2192}빨강)],
  [#key("Tab")],         [활성 패널 전환 (A \u{2194} B)], [마우스가 올라간 패널의 worst 블록 순회],
  [#key("Shift+Tab")],   [활성 패널 전환], [worst 블록 역순 순회],
  [#key("[") / #key("]")], [amplify \u{00B1}0.5 (AlphaBlend 에서는 alpha \u{00B1}1%)], [img2 의 worst 블록만 이전/다음],
  [#key("Shift+[") / #key("Shift+]")], [threshold \u{00B1}1 (AlphaBlend 에서는 alpha \u{00B1}10%)], [img3 의 worst 블록만 이전/다음],
  [#key("Ctrl+D")],      [diff 픽셀 리스팅 창], [worst 블록 리스트 창 (행 클릭 \u{2192} 3패널 점프)],
  [#key("Ctrl+B")],      [동작 없음], [worst 블록 사각형 표시 토글],
  [#key("Ctrl+W")],      [동작 없음], [승패 맵 토글 (파랑 = img2 우세, 주황 = img3 우세)],
  [#key(",")],           [A/B 블링크 비교기], [3-way 블링크 (orig \u{2192} img2 \u{2192} img3)],
  [#key(";") / #key("a")], [활성 패널의 시퀀스만 이동], [원본 기준 이동 + img2/img3 가 파일명으로 따라옴],
)

모드와 무관하게 같은 동작을 하는 키도 함께 정리합니다. #key("B") 는 두 모드 모두
패널 테두리 토글이고, #key("Ctrl+S") 는 두 모드 모두 통계 창, #key("Shift+Ctrl+S") 는
저장 창입니다. 즉 #key("S") 계열에서 헷갈리는 것은 *수식키 없는* #key("S") 하나뿐입니다.

#warn[
`--comp` 모드에서는 #key("S") 가 패널 교환에 배정되므로 *키보드로 sync 를 토글할 수
없습니다*. 필요하면 #key("U") 로 UI 오버레이를 켜고 View 메뉴의 "Sync Viewports"
체크박스를 쓰십시오.
]

=== `--comp` 가 매 프레임 강제로 끄는 상태

`--comp` 는 3영상 레이아웃을 지키기 위해 매 프레임 아래 상태를 초기화합니다. 키를
눌러 켜더라도 다음 프레임에 곧바로 꺼지므로 "눌렀는데 아무 일도 안 일어난다"로
보입니다. 정상 동작입니다.

#table(
  columns: (auto, 1fr),
  [*강제 해제되는 것*], [*관련 키*],
  [Diff 모드 전부], [#key("Ctrl+0")\~#key("Ctrl+7"), #key("Ctrl+9")],
  [A/B 스왑], [#key("Shift+Space")],
  [A/B 블링크], [#key(",") \u{2014} 대신 3-way 블링크로 재정의됨],
  [Overlay/Blend], [#key("O")],
)

== 문제 해결 Q&A

=== Q1. OpenGL 초기화에 실패하거나 원격 화면에서 창이 안 뜹니다

av 는 OpenGL 3.3 Core 를 먼저 시도하고, 실패하면 스스로 SDL 소프트웨어 렌더러로
내려갑니다. 폴백은 세 단계 모두에서 일어나며 각각 표준 오류에 한 줄씩 남습니다.

```
[av] GL window creation failed: ... — falling back to software renderer
[av] GL context creation failed: ...  — falling back to software renderer
[av] gladLoadGL failed — falling back to software renderer
```

성공하면 `OpenGL 4.6 ... / Renderer: ...` 가, 소프트웨어 모드면
`Using SDL software renderer (no OpenGL)` 가 출력됩니다. 폴백조차 실패하면 종료
코드 `2` 로 끝납니다.

처음부터 소프트웨어 렌더러를 쓰려면 `--software` 를 붙입니다.

```
av --software golden.png hw.png
```

환경변수 `SSH_CONNECTION` 이 설정돼 있으면 (즉 SSH X11 forwarding 이면) `--software`
없이도 자동으로 소프트웨어 모드로 전환하고 다음을 출력합니다.

```
[av] X11 forwarding detected (SSH) — using software renderer
```

#note[
소프트웨어 모드에서는 Linux X11 오류 핸들러도 함께 설치되어 MIT\u{2011}SHM/GLX 오류를
무시합니다. 원격 화면에서 자주 보이던 X 오류 종료가 이 경로로 방지됩니다.
]

=== Q2. SSH 로 접속한 서버라 GUI 를 못 씁니다. 수치만 뽑고 싶습니다

창을 아예 열지 않는 헤드리스 모드를 쓰십시오. `--metrics` 는 PSNR/SSIM/FLIP/MSE/MAE
를 CSV 로 출력하고 종료합니다.

```
av --metrics golden.png hw.png
av --metrics --pair --format json /work/golden/f0001.png /work/hw_out
```

블록 단위 비교, diff PNG 내보내기, 균일도, CI 게이트까지 모두 헤드리스로 가능합니다.
자세한 사용법과 종료 코드 규약은 8장을 참조하십시오.

=== Q3. 두 영상 크기가 달라 비교가 안 됩니다 (종료 코드 5)

헤드리스 비교는 크기·포맷이 다르면 계산을 포기하고 종료 코드 `5` 를 돌려줍니다.

```
av --metrics 1920x1080.png 1280x720.png ; echo $?
# [metrics] size/format mismatch
# 5
```

`--diff-out` 은 `[diff-out] size mismatch: 1920x1080 vs 1280x720`, `--comp-out` 은
`[comp] size/format mismatch vs orig (img2:BAD img3:ok)` 를 남기고 역시 `5` 입니다.
"포맷 불일치"에는 한쪽만 HDR/float 인 경우도 포함됩니다.

GUI 에서는 다르게 동작합니다. Image Info 창의 PSNR 은 `N/A (size/format mismatch)`
로 정직하게 표시하지만, diff 캔버스와 통계 창은 두 영상을 *선형 인덱스 기준으로
겹쳐* 계산하므로 숫자가 나오긴 합니다. 그 숫자는 의미가 없습니다.

#warn[
크기가 다르면 먼저 크롭/리사이즈로 해상도를 맞추십시오. DDI 검증에서는 보상 IP 가
패널 해상도 그대로 출력했는지부터 확인하는 편이 빠릅니다. `--pair` 로 시퀀스를 돌릴
때 특정 프레임만 크기가 다르면 그 프레임의 행만 `mismatch` 상태로 표시되고 나머지는
정상 집계됩니다.
]

=== Q4. HDR 이미지에 NaN 이나 Inf 가 섞인 것 같습니다

GUI 에서는 #key("/") 를 눌러 불량 픽셀 오버레이를 켭니다. NaN/Inf/음수/`1.0` 초과
픽셀이 표시됩니다 (float 이미지에만 의미가 있습니다).

배치로 확인하려면 `--validate` 를 씁니다. 클래스별 개수를 CSV 로 내고, 처음 발견한
20개의 좌표를 주석으로 덧붙입니다.

```
av --validate hdr_out.hdr
# class,count
# nan,3
# inf,0
# negative,128
# superwhite,4096
# # first offenders: class,x,y,channel
# # nan,512,300,R
```

NaN 또는 Inf 가 하나라도 있으면 종료 코드 `8` 로 끝나므로 CI 게이트로 바로 쓸 수
있습니다. 음수나 `1.0` 초과는 정상 HDR 에서도 나타날 수 있어 종료 코드를 바꾸지
않습니다. 8비트 이미지를 넣으면 구조적으로 불가능하므로 전부 `0` 을 출력하고
`8-bit image (no float data) — nothing to validate` 를 알린 뒤 `0` 으로 끝납니다.

=== Q5. 16비트 PPM 의 픽셀 값이 이상하게 보입니다

av 는 PPM(P2/P3/P5/P6)을 읽을 때 원본 정수값을 `pixels_orig` 에 따로 보관하고,
화면 표시용 8비트 버퍼와 정밀 연산용 float 버퍼를 추가로 만듭니다. 픽셀 값 풍선과
diff 리스팅은 *원본 정수값*을 우선 사용하므로, 10비트 PPM 이면 `0`\~`1023` 범위의
값이 그대로 나옵니다. 우선순위 규칙은 3장에서 자세히 설명합니다.

값이 `0`\~`255` 로 눌려 보인다면 대개 다음 중 하나입니다.

- 파일에 `maxval` 이 `255` 로 적혀 있다 \u{2014} 헤더가 8비트로 저장된 것입니다.
- 소프트웨어 렌더러로 실행 중이다 \u{2014} *화면*은 8비트 텍스처로 그려집니다. 값 풍선은
  여전히 원본 정수값을 보여 주므로 숫자를 믿으십시오.
- 확장자가 `.ppm`/`.pgm` 이 아니라 다른 형식이다 \u{2014} PPM 경로를 타지 않으면
  `pixels_orig` 가 만들어지지 않습니다.

#warn[
`--no-color-mgmt` 와 `--profile` 은 CLI 파서가 받아들이기는 하지만, 현재 빌드의 표시
파이프라인에서 이 값을 소비하는 코드가 없습니다. 즉 두 옵션을 붙여도 화면 결과는
바뀌지 않습니다. 밝기·색이 예상과 다르다면 LUT/CDL 적용 여부(#key("'"))와 채널 모드
(#key("Shift+C") 로 RGB 복귀)를 먼저 확인하십시오. 9장을 함께 참조하십시오.
]

=== Q6. `--comp` 인데 diff 키가 안 먹습니다

정상입니다. `--comp` 는 인자 파싱 단계에서 diff 모드를 `none` 으로 되돌리고, 실행 중
매 프레임 다시 `none` 으로 강제합니다. 3영상 레이아웃(`orig | img2 | img3`)에는
diff 패널이 들어갈 자리가 없기 때문입니다. 두 영상 diff 가 필요하면 `--comp` 없이
두 장만 여십시오.

```
av --comp golden.png fw.png hw.png    # diff 불가, 블록 비교 전용
av -d golden.png hw.png               # 두 장 diff
```

#warn[
세 번째 영상 로드에 실패하면 3패널 레이아웃 조건이 깨져 화면은 2패널로 떨어지지만
`--comp` 상태는 그대로라 diff 는 여전히 강제 해제됩니다. "2패널인데 diff 도 안 되는"
상태가 보이면 시작 로그에서 `Failed to load image C:` 를 확인하십시오.
]

=== Q7. `--comp` 인데 블록 박스가 안 보입니다

다음 순서로 확인하십시오.

#table(
  columns: (auto, 1fr),
  [*확인*], [*내용*],
  [토글 상태], [#key("Ctrl+B") 로 worst 박스를 껐을 수 있습니다. 기본값은 켜짐입니다],
  [패널], [worst 박스와 상세 풍선은 *비교 패널(img2/img3)* 전용입니다. 왼쪽 원본 패널에는 박스가 그려지지 않습니다],
  [개수], [`--num_blk` 가 작으면 그만큼만 그립니다 (기본 `16`). `--num_blk 64` 로 늘려 보십시오],
  [줌], [fit 줌에서 `8x8` 블록은 화면에서 몇 픽셀에 불과해 2px 두께 사각형끼리 겹쳐 뭉개져 보입니다. #key("1")\~#key("8") 로 확대하거나, #key("Tab") 으로 순회하면 뷰포트가 해당 블록으로 자동 이동합니다],
  [그리드], [#key("G") 그리드 선은 셀이 화면에서 *4px 미만*이면 아예 그리지 않습니다. fit 줌에서 `--grid 16x16` 이 안 보이는 것은 이 규칙 때문입니다],
  [통계], [영상을 바꾸거나 #key("S") 로 교환한 직후에는 블록 통계가 무효화되고 다음 프레임에 다시 계산됩니다],
)

블록 번호 라벨(`#1`, `#3P` 등)은 박스가 화면에서 가로 22px, 세로 14px 을 넘을 때만
표시됩니다. 마젠타 색과 `P` 접미사는 피크 픽셀 오차로 뽑힌 블록입니다 (5장 참조).

=== Q8. 키를 눌렀는데 아무 반응이 없습니다

원인은 대개 셋 중 하나입니다.

#table(
  columns: (auto, 1fr),
  [*원인*], [*확인과 대처*],
  [모드별 재정의], [이 장의 대조표를 확인하십시오. #key("S") #key("G") #key("Tab") #key("[") #key("]") #key("Ctrl+D") 는 `--comp` 에서 뜻이 다릅니다],
  [ImGui 입력 포커스], [히스토그램·저장 창의 입력란에 커서가 있으면 수식키 없는 평문 키가 그 창으로 먹힙니다. 패널을 한 번 클릭하거나 #key("Esc") 로 창을 닫으십시오. #key("Ctrl") / #key("Cmd") 조합은 항상 통과합니다],
  [활성 패널], [#key(";") #key("a") 같은 시퀀스 키는 *활성 패널*에만 적용됩니다. #key("Tab") 으로 바꾸고, 테두리가 굵은 쪽이 활성입니다 (7장 참조)],
)

또 하나 알아 둘 것은 복사 모드입니다. #key("Ctrl+C") 를 누르면 av 는 "어느 이미지를
복사할지" 기다리는 상태로 들어가고, 이어서 누른 키가 `1`/`2`/`3` 이면 각각 A/B/Diff 를
PNG 로 클립보드에 복사합니다. 그 외의 키를 누르면 대기 상태만 풀리고 그 키는 원래
동작을 그대로 수행하므로 키를 잃어버리지는 않습니다. #key("Esc") 만은 아무 동작 없이
대기 상태를 취소합니다.

=== Q9. `Ctrl+D` 를 눌러도 diff 픽셀 리스팅 창이 뜨지 않습니다

리스팅 목록은 *diff 렌더 경로에서만* 계산됩니다. diff 모드가 꺼져 있으면 목록이
계산되지 않았으므로 창은 열리자마자 스스로 닫힙니다. 두 영상이 완전히 동일할 때도
같은 이유로 닫힙니다.

```
av -d golden.png hw.png    # diff 모드로 띄운 뒤 Ctrl+D
```

채널 모드가 R/G/B 중 하나면 "그 채널만 동일"해도 창이 닫힙니다. #key("Shift+C") 로
RGB 로 되돌린 뒤 다시 시도하십시오.

=== Q10. 이 파일 형식은 왜 안 열립니까

시퀀스 스캔과 파일 인식에 쓰이는 확장자 목록은 다음 8종이며 대소문자는 무시합니다.

```
.png  .jpg  .jpeg  .bmp  .tga  .pgm  .ppm  .hdr
```

`.tif`/`.tiff`, `.exr`, `.webp`, `.dng` 등은 지원하지 않습니다. PPM 계열은 P2/P3
(ASCII) 와 P5/P6 (binary) 를 자체 파서로 읽으며 `maxval` 은 `65535` 까지 허용합니다.
`.hdr` 은 Radiance RGBE 로 읽어 float 버퍼를 만듭니다.

로드에 실패하면 표준 오류에 이유가 남고, *기존 이미지는 그대로 유지*됩니다.

```
[image_loader] failed to load: shot.tiff — unknown image type
```

#tip[
DDI 검증 덤프가 TIFF 나 16비트 PNG 로 나온다면, 전처리로 PPM(P6, `maxval 1023`)이나
PNG 로 변환해 두는 편이 av 의 원본 정수값 표시와 잘 맞습니다.
]

=== Q11. 한글 파일명이 깨지고 글자가 작게 나옵니다

av 의 UI 폰트는 ImGui 기본 폰트(ProggyClean 13px)와 Roboto\u{2011}Medium(26px / 18px)
두 가지뿐이며 *CJK 글리프를 포함하지 않습니다*. 따라서 파일명 토스트나 경로에 한글이
들어가면 해당 글자는 표시되지 않습니다. 영문·숫자 파일명을 쓰는 것이 유일한
해결책입니다 (파일을 읽고 비교하는 동작 자체는 정상입니다).

Roboto 폰트 경로는 *빌드 시점의 절대 경로*로 박힙니다. 빌드 트리를 지우거나 다른
장비로 실행 파일만 복사하면 폰트 로드가 실패하고 기본 폰트로 대체되어, Image Info
창과 픽셀 값 풍선의 글자가 갑자기 작아집니다. 기능에는 영향이 없습니다. 큰 글자가
필요하면 빌드 트리를 유지한 채 설치하십시오.

=== Q12. 설정이 저장되지 않습니다

#table(
  columns: (auto, 1fr),
  [*증상*], [*원인*],
  [강제 종료 후 초기화], [`kill -9`, 크래시, 로그아웃 시 저장 루틴이 실행되지 않습니다. #key("Q") 나 창 닫기로 종료하십시오],
  [폴더를 바꾸면 초기화], [`HOME` 미설정 (주로 Windows cmd/PowerShell). Q1 위쪽의 경로 표를 참조하십시오],
  [아무리 해도 안 바뀜], [`.av.ini` 가 읽기 전용이거나 홈 디렉토리에 쓰기 권한이 없습니다. 저장 실패는 오류 메시지 없이 조용히 무시됩니다],
  [sync/창 모드만 안 남음], ["저장은 되지만 반영되지 않는 두 키" 절 참조 \u{2014} 설계상 매번 CLI 기본값으로 시작합니다],
  [여러 창을 띄웠을 때], [마지막에 종료한 인스턴스가 파일을 덮어씁니다],
)

== 성능 팁

=== 큰 이미지와 시퀀스

av 는 디코드한 이미지를 LRU 캐시에 최대 *8장*까지 보관하고, 파일의 수정 시각과
크기로 유효성을 검사합니다. 덕분에 #key(";") / #key("a") 로 앞뒤를 오갈 때 최근 8장은
디스크 재디코드 없이 즉시 뜹니다. 반대로 말하면 메모리도 8장분을 차지합니다.

#table(
  columns: (auto, auto, 1fr),
  [*입력*], [*이미지 1장당 CPU 버퍼*], [*설명*],
  [8비트 PNG/JPG], [약 4 bytes/px], [RGBA8 한 벌],
  [10\~16비트 PPM], [약 26 bytes/px], [RGBA8 + 원본 uint16 RGB + RGBA32F],
  [`.hdr` (float)], [약 16 bytes/px], [RGBA32F (소프트웨어 모드에서는 RGBA8 도 추가)],
)

4K(3840\u{00D7}2160) 10비트 PPM 한 장이 대략 200MB 이므로, 캐시가 가득 차면 1.5GB 이상을
쓸 수 있습니다. GPU 텍스처도 캐시 항목마다 하나씩 잡힙니다. 메모리가 빠듯한
장비에서 대용량 시퀀스를 돌릴 때는 GUI 대신 헤드리스 `--metrics --pair` 나
`--comp-batch` 를 쓰는 편이 훨씬 가볍습니다 (8장 참조).

#tip[
프레임을 넘기면서 같은 영역을 확대해 보려면 `--zoom` 으로 배율을 고정하십시오.
시퀀스 로드마다 fit 으로 되돌아가지 않고 지정 배율 + 중앙 정렬을 유지합니다.
]

=== 무거운 계산이 언제 일어나는가

#table(
  columns: (auto, 1fr),
  [*작업*], [*시점과 특성*],
  [SSIM / FLIP 히트맵], [모드 진입이나 영상 변경 시 *워커 스레드*에서 계산. 상태바에 `SSIM: computing…` 이 뜨는 동안에도 화면은 계속 반응합니다],
  [`--comp` 블록 스캔], [영상 로드/교환 후 첫 프레임에 1회. 이후 캐시되어 토글은 즉시 반응합니다],
  [diff 픽셀 리스팅], [diff 렌더 중 지연 계산. 영상 크기와 콘텐츠 버전이 같으면 재사용],
  [창 드래그], [창을 옮기는 동안 렌더를 건너뜁니다. 이동이 멈추고 100ms 뒤 자동 재개],
)

=== 소프트웨어 렌더러 사용 시 제약

`--software` 또는 SSH 자동 감지로 소프트웨어 모드가 되면 다음이 달라집니다.

#table(
  columns: (auto, 1fr),
  [*항목*], [*소프트웨어 모드에서의 동작*],
  [리샘플링], [매 프레임 CPU 로 패널 크기만큼 다시 그립니다. *창이 클수록 느려집니다*],
  [LUT / CDL], [출력 픽셀마다 CPU 로 적용. GPU 3D 텍스처 경로를 쓰지 않습니다],
  [HDR 표시], [float 값을 `0`\~`1` 로 클램프한 8비트 텍스처로 표시. 값 읽기는 원본 유지],
  [16비트 PPM 표시], [8비트 텍스처로 표시. 값 읽기는 원본 정수값 유지],
  [diff / overlay / 채널], [CPU 경로로 모두 지원 \u{2014} 기능이 빠지지는 않습니다],
)

#tip[
원격 화면에서 반응이 느리면 전체화면 대신 창을 작게 띄우십시오. 기본은 테두리 없는
최대화 창이라 화면 전체를 CPU 로 그립니다.

```
av --software --windowed --geometry 1024x640 golden.png hw.png
```
]
