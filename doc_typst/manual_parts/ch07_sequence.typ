#import "_common.typ": *

= 이미지 시퀀스와 파일 입출력

DDI 보상 IP 검증은 정지 영상 한 장으로 끝나지 않습니다. 보통 수십\~수백 프레임의
테스트 패턴 시퀀스를 원본 디렉토리와 보상 결과 디렉토리로 나누어 뽑아 놓고,
"몇 번째 프레임에서 화질이 무너지는가"를 찾아내야 합니다. 이 장에서는 av 가
디렉토리를 시퀀스로 다루는 방식, 두 패널을 같은 파일명으로 묶는 `--pair`,
자동 재생, 그리고 결과를 파일·클립보드로 내보내는 방법을 다룹니다.

== 디렉토리 시퀀스 \u{2014} 자동 구성과 정렬 규칙

av 는 별도의 "시퀀스 열기" 기능이 없습니다. *이미지 한 장을 여는 순간* 그 파일이
들어 있는 디렉토리를 스캔해서 시퀀스 목록을 자동으로 만듭니다. CLI 인자로 연 경우,
파일 다이얼로그로 연 경우, 창에 드래그앤드롭한 경우, `;`/`a` 로 이동한 경우 모두
동일한 로더를 거치므로 예외가 없습니다.

시퀀스에 포함되는 파일은 확장자로 걸러집니다 (대소문자 무시).

#table(
  columns: (auto, 1fr),
  [*항목*], [*값*],
  [스캔 대상], [연 파일의 부모 디렉토리 (경로에 디렉토리가 없으면 `.`)],
  [포함 확장자], [`.png` `.jpg` `.jpeg` `.bmp` `.tga` `.pgm` `.ppm` `.hdr`],
  [하위 디렉토리], [스캔하지 않음 (1단계, 정규 파일만)],
  [정렬 기준], [파일명(basename)의 바이트 코드포인트 순 \u{2014} `LC_ALL=C` 와 동일],
  [보관 단위], [패널 A / 패널 B 각각 독립된 목록과 현재 인덱스],
)

정렬은 *natural sort 가 아닙니다*. 대문자가 소문자보다 앞서고, 숫자를 자릿수로
묶지 않습니다. 즉 `f10.png` 가 `f2.png` 보다 *앞*에 옵니다. 이 규칙은 av 가 참조하는
`input_img_file.toml` 의 순서 규칙과 일치시키기 위한 의도적인 선택입니다.

#warn[
프레임 번호는 반드시 제로패딩하십시오. `frame_1.png` 부터 `frame_100.png` 까지
뽑으면 탐색 순서가 `frame_1, frame_10, frame_100, frame_11, ...` 이 됩니다.
`frame_0001.png` 형태로 뽑으면 파일명 순서 = 프레임 순서가 보장됩니다.
]

현재 위치는 상태바에 항상 표시됩니다. 패널 A 가 100장 중 3번째라면 상태바에
`A [3/100]` 이 초록색으로 나오고, B 패널에도 시퀀스가 있으면 `B [3/100]` 이
이어서 표시됩니다. 인덱스는 1부터 셉니다.

== 시퀀스 탐색 키

#table(
  columns: (auto, 1fr),
  [*키*], [*동작*],
  [#key(";")], [활성 패널의 시퀀스에서 *다음* 이미지],
  [#key("a")], [활성 패널의 시퀀스에서 *이전* 이미지],
  [#key("N") / #key("Shift+N")], [다음 / 이전 (레거시 핫키, `;`/`a` 와 완전히 동일)],
  [#key("Tab")], [활성 패널 전환 (A \u{2194} B)],
  [#key("Alt") \u{002B} 마우스휠], [커서가 올라가 있는 패널의 시퀀스를 위/아래로 이동],
)

=== 어떤 패널이 움직이는가

`;` 와 `a` 는 *활성 패널(active panel)* 의 시퀀스만 움직입니다. 활성 패널은
#key("Tab") 으로 바꾸며, 화면에서는 해당 패널의 테두리가 굵어지는 것으로
구분합니다 (활성 4px, 비활성 2px). #key("B") 로 테두리를 꺼 둔 상태라면 활성
패널이 보이지 않으므로, 시퀀스 작업 중에는 테두리를 켜 두는 편이 안전합니다.

#key("Alt") \u{002B} 휠은 활성 패널과 무관하게 *마우스 커서 아래 패널*을 움직입니다.
왼손으로 키를 누르지 않고 오른손 휠만으로 A 와 B 를 번갈아 넘길 때 편리합니다.
#key("Alt") 없이 휠을 돌리면 평소대로 줌입니다 (2장 참조).

=== 순환과 피드백

목록의 마지막에서 #key(";") 를 누르면 처음으로, 첫 번째에서 #key("a") 를 누르면
마지막으로 순환합니다. 경계에서 멈추지 않으므로 100프레임 시퀀스를 계속 넘겨도
막히지 않습니다.

로딩이 끝나면 화면 *상단 중앙*에 파일명 토스트가 *1.5초* 동안 뜹니다. 형식은
`A:  frame_0042.png` 처럼 패널 라벨과 basename 입니다. 로드에 실패하면 배경이
붉게 바뀌고 `A  (failed):  frame_0042.png` 로 표시되며, *이전 이미지는 그대로
유지*됩니다(비파괴 로드). 전체화면이나 테두리 없는 모드처럼 타이틀바가 없는
상태에서도 지금 몇 번 프레임을 보고 있는지 알 수 있게 하기 위한 장치입니다.

#note[
시퀀스로 다음 영상을 로드해도 줌 설정은 깨지지 않습니다. `--zoom` 이나 `.av.ini`
로 고정 배율이 지정돼 있으면 그 배율을 유지한 채 가운데 정렬하고, 지정이 없으면
(기본값 = fit) 새 영상 크기에 맞춰 다시 맞춥니다. 400% 로 확대한 채 프레임을
넘기며 같은 영역을 보려면 `--zoom 4` 로 띄우십시오.
]

#warn[
히스토그램·통계 같은 ImGui 창의 입력란에 키보드 포커스가 있으면 `;` `a` `N` 같은
평문 키가 그 창으로 먹힙니다. 탐색이 안 되면 패널을 한 번 클릭하거나 #key("Esc")
로 창을 닫은 뒤 다시 누르십시오.
]

== A 와 B 는 서로 다른 디렉토리를 독립으로 탐색합니다

패널 A 와 B 는 각자의 파일 목록과 인덱스를 따로 들고 있습니다. 왼쪽에 원본
디렉토리를, 오른쪽에 보상 결과 디렉토리를 띄워 놓고 한쪽만 넘길 수 있다는
뜻입니다.

```
av /work/golden/frame_0001.png /work/hw_out/frame_0001.png
```

이 상태에서 #key("Tab") 으로 B 를 활성화한 뒤 #key(";") 를 한 번 누르면 오른쪽만
다음 프레임으로 갑니다. 실무에서 이 독립성이 필요한 대표적인 경우는 다음과
같습니다.

- *파이프라인 지연 보정*: 보상 IP 가 1프레임 지연되어 출력을 뽑은 경우, B 만 한 칸
  앞으로 밀어 프레임 정렬을 맞춘 뒤 비교합니다.
- *파일명 규칙이 다른 덤프*: 원본은 `frame_0001.png`, HW 덤프는 `cap_0001.png` 처럼
  이름 체계가 다르면 같은 이름 짝짓기(`--pair`)를 쓸 수 없으므로 독립 탐색으로
  수동 정렬합니다.
- *기준 프레임 고정*: A 에 플랫 필드 기준 영상을 고정해 두고 B 만 넘기며 무라를
  비교합니다.

반대로 프레임 정렬이 이미 맞고 파일명도 같다면, 한쪽만 넘어가는 것은 오히려
사고의 원인입니다. 그럴 때 쓰는 것이 다음 절의 `--pair` 입니다.

== `--pair` \u{2014} 같은 파일명으로 두 디렉토리를 묶어 이동

`--pair` 는 A 의 디렉토리를 기준 시퀀스로 삼고, *같은 파일명*을 B 의 디렉토리에서
찾아 항상 함께 로드합니다. `;`/`a` 를 아무리 눌러도 두 패널의 프레임이 어긋나지
않습니다.

#fig_sequence_sync()

```
av --pair /work/golden/frame_0001.png /work/hw_out
```

두 번째 인자는 파일이어도 되고 디렉토리여도 됩니다. 파일을 주면 그 파일의 부모
디렉토리가 비교 디렉토리로 쓰입니다.

```
av --pair /work/golden/frame_0001.png /work/hw_out/frame_0001.png   # 위와 동일
```

#table(
  columns: (auto, 1fr),
  [*규칙*], [*동작*],
  [기준], [항상 A(왼쪽) 디렉토리가 시퀀스를 구동. #key("Tab") 으로 B 를 활성화해도 `;`/`a` 는 A 기준],
  [짝짓기], [A 의 basename 을 B 디렉토리에서 그대로 찾음 (확장자 포함 완전 일치)],
  [디렉토리 검증], [A 와 B 의 디렉토리가 같으면 창을 띄우기 전에 오류 출력 후 종료],
  [인자 개수], [경로 2개 필수. 하나만 주면 오류 후 종료],
  [짝이 없을 때], [B 패널을 비우고 안내 문구 표시 (아래 참조)],
)

=== 짝이 없는 프레임

B 디렉토리에 같은 이름의 파일이 없으면 av 는 조용히 넘어가지 않고 B 패널을 비운 뒤
다음과 같이 표시합니다.

```
No matching image:
frame_0057.png
in /work/hw_out
```

이 화면 자체가 "HW 덤프에서 57번 프레임이 빠졌다"는 리포트입니다. 프레임 누락은
보상 IP 검증에서 흔한 사고이므로, 시퀀스를 한 번 훑어보는 것만으로 결손을 잡아낼
수 있습니다.

#warn[
디렉토리 검증은 창을 열기 전에(fail-fast) 수행됩니다. A 와 B 가 같은 디렉토리면
`--pair: both directories are the same (...). Nothing to compare.` 를 stderr 로
출력하고 즉시 종료하므로, 스크립트에서 경로를 잘못 넘긴 것을 바로 알 수 있습니다.
]

=== 헤드리스 지표와 조합

`--pair` 는 GUI 전용 옵션이 아닙니다. `--metrics` 와 함께 쓰면 A 디렉토리의 모든
프레임을 순회하며 프레임당 한 줄씩 CSV 를 뽑습니다.

```
av --pair --metrics /work/golden/frame_0001.png /work/hw_out
```

짝이 없는 프레임은 `missing`, 디코드 실패는 `decode_error`, 크기/포맷 불일치는
`mismatch` 로 상태가 찍히므로 회귀 프레임과 결손 프레임을 한 번에 걸러낼 수
있습니다. 컬럼 구성과 `--fail-psnr` 같은 CI 게이트는 8장을 참조하십시오.

== `--comp` 의 3디렉토리 미러링

세 영상 블록 비교 모드(`--comp`, 5장 참조)에서도 시퀀스 탐색이 동작합니다.
동작 원리는 `--pair` 와 같지만 대상이 셋입니다.

```
av --comp /work/golden/f_0001.png /work/fw_out/f_0001.png /work/hw_out/f_0001.png
```

#table(
  columns: (auto, 1fr),
  [*대상*], [*시퀀스 동작*],
  [orig (원본)], [#key(";") / #key("a") 가 이 디렉토리의 목록을 기준으로 이동],
  [img2], [현재 img2 경로의 디렉토리에서 *같은 파일명*을 자동 로드],
  [img3], [현재 img3 경로의 디렉토리에서 *같은 파일명*을 자동 로드],
)

`--pair` 와 마찬가지로, #key("Tab") 으로 다른 패널을 만져도 `;`/`a` 는 언제나 원본
디렉토리를 기준으로 움직입니다. 새 프레임이 로드되면 블록 통계가 무효화되어
다음 프레임에 대해 worst 블록 순위와 PSNR 이 자동으로 다시 계산됩니다.

#warn[
`--comp` 에서 img2 나 img3 디렉토리에 같은 이름의 파일이 없으면 해당 패널의 영상은
*해제되어 빈 패널*이 됩니다. 이때는 `--pair` 처럼 "No matching image" 안내가 아니라
`(no image)` 로만 보이며, 세 영상이 모두 갖춰지지 않았으므로 블록 비교 통계도
계산되지 않습니다. 프레임을 한 칸 더 넘겨 세 파일이 모두 있는 프레임에 도달하면
정상 상태로 복귀합니다.
]

#tip[
FW 모델 출력과 HW 실측 덤프를 원본과 동시에 프레임 단위로 훑을 때 이 미러링이
가장 강력합니다. `;` 로 프레임을 넘기면서 #key("Ctrl+B") 의 worst 블록 박스가
프레임마다 어디로 이동하는지 보면, 특정 패턴에서만 터지는 보상 오류를 빠르게
좁힐 수 있습니다.
]

== 슬라이드쇼 (자동 재생)

#table(
  columns: (auto, 1fr),
  [*키*], [*동작*],
  [#key("Shift+A")], [슬라이드쇼 자동 재생 토글],
  [#key("Shift+Up")], [재생 간격 \u{002B}1초],
  [#key("Shift+Down")], [재생 간격 \u{2212}1초],
)

#key("Shift+A") 를 누르면 지정된 간격마다 #key(";") 를 누른 것과 같은 동작이
자동으로 반복됩니다. 재생 대상 패널은 *토글한 순간의 활성 패널*로 고정되므로,
재생 중에 #key("Tab") 을 눌러도 재생 대상은 바뀌지 않습니다. 다시 #key("Shift+A")
를 누르면 정지합니다.

#table(
  columns: (auto, 1fr),
  [*항목*], [*값*],
  [기본 간격], [`3.0`초],
  [조절 단위], [`1.0`초],
  [범위], [`0.5`초 \u{007E} `10.0`초 (경계에서 클램프)],
  [영속화], [되지 않음. av 를 다시 실행하면 `3.0`초로 복귀],
)

재생 중에는 상태바에 재생 삼각형 아이콘과 함께 `2.4s / 3.0s` 형태로
(남은 시간 / 설정 간격) 이 청록색으로 표시됩니다.

#warn[
#key("Shift+Up") / #key("Shift+Down") 은 *슬라이드쇼가 켜져 있을 때만* 간격을
조절합니다. 꺼져 있으면 같은 키가 원래 기능인 화면 팬(`--pan-step` 만큼 상하 이동)
으로 동작합니다. 간격이 안 바뀐다면 먼저 #key("Shift+A") 로 재생을 켜십시오.
]

#tip[
`--pair` 나 `--comp` 와 함께 쓰면 슬라이드쇼도 원본 디렉토리 기준으로 진행되므로,
두(또는 세) 디렉토리가 프레임 단위로 정렬된 채 자동 재생됩니다. 100프레임 시퀀스를
`0.5`초 간격으로 돌리며 눈으로 훑어 "튀는 프레임"을 먼저 표시해 두고, 그 구간만
`;`/`a` 로 정밀 검사하는 방식이 효율적입니다.
]

== 파일 열기

#table(
  columns: (auto, 1fr),
  [*방법*], [*동작*],
  [#key("Shift+Ctrl+O") (macOS: #key("Shift+Cmd+O"))], [네이티브 열기 다이얼로그 \u{2014} *활성 패널*에 로드],
  [메뉴 `File > Open Image A...`], [패널 A 에 로드],
  [메뉴 `File > Open Image B...`], [패널 B 에 로드],
  [메뉴 `File > Open Image...`], [활성 패널에 로드 (단축키와 동일)],
  [드래그앤드롭], [A 가 비어 있으면 A, 아니면 B 에 로드],
)

다이얼로그의 파일 필터는 `png;jpg;jpeg;bmp;tga;hdr;ppm;pgm` 과 `All files` 두 가지입니다.

`Open Images` 창에는 `Clear other image when loading single` 체크박스가 있습니다.
켜면 한쪽을 새로 열 때 반대쪽 패널을 비웁니다. 기본은 꺼짐이며, 시퀀스 탐색으로
로드할 때는 이 옵션과 무관하게 반대쪽을 건드리지 않습니다.

#note[
어떤 경로로 열든 로드 성공 시에만 패널이 교체됩니다. 손상된 파일을 열어도 기존
영상과 뷰포트, 시퀀스 목록은 그대로 남고 붉은 실패 토스트만 뜹니다. 또 로드가
성공하면 그 파일의 디렉토리로 시퀀스가 다시 구성되므로, 다른 디렉토리의 파일을
열면 그 순간부터 새 디렉토리를 탐색하게 됩니다.
]

== 저장 \u{2014} `Save Images` 창

#key("Shift+Ctrl+S") (macOS: #key("Shift+Cmd+S")) 또는 메뉴 `File > Save Images...`
로 저장 창을 토글합니다. 이 단축키는 *문맥 인식*입니다.

#table(
  columns: (auto, 1fr),
  [*열려 있는 창*], [*뜨는 저장 창*],
  [히스토그램], [차트 저장 창 (PNG \u{002B} CSV, 6장 참조)],
  [H-Line / V-Line Cut], [차트 저장 창 (PNG \u{002B} CSV, 6장 참조)],
  [통계 창], [통계 저장 창 (6장 참조)],
  [그 외], [`Save Images` \u{2014} 이 절에서 설명],
)

`Save Images` 창은 세 개의 저장 대상을 한 화면에서 다룹니다.

#table(
  columns: (auto, 1fr),
  [*대상*], [*활성 조건과 내용*],
  [`Image A`], [패널 A 의 영상 원본 픽셀],
  [`Image B`], [패널 B 의 영상 원본 픽셀],
  [`Diff`], [A·B 가 모두 로드되고 diff 모드가 `none` 이 아닐 때만 활성. 현재 diff 모드와 amplify 를 그대로 반영한 결과 영상],
)

각 행은 체크박스, 경로 입력칸, `Save` 버튼으로 구성됩니다. 하단의
`Save All Checked` 를 누르면 체크된 항목을 한꺼번에 저장하고, 하나라도 실패하면
그 지점에서 멈추고 붉은 오류 메시지를 표시합니다. 성공하면 초록색으로
`Saved: <파일명>` 이 뜹니다.

=== 기본 경로

#table(
  columns: (auto, 1fr),
  [*대상*], [*기본 경로*],
  [`Image A`], [원본과 같은 디렉토리에 `<A의 stem>_save.png`],
  [`Image B`], [원본과 같은 디렉토리에 `<B의 stem>_save.png`],
  [`Diff`], [A 의 디렉토리에 `<A stem>_vs_<B stem>_diff.png`],
)

영상이 로드돼 있지 않으면 각각 `./image_a_save.png`, `./image_b_save.png` 가
쓰입니다. 포맷을 바꾸면 세 경로의 확장자가 자동으로 따라 바뀝니다.

=== 포맷 옵션

#table(
  columns: (auto, 1fr),
  [*옵션*], [*값*],
  [`Format`], [`PNG` (기본) \u{007C} `BMP` \u{007C} `PPM`],
  [`Mode` (PPM 전용)], [`Binary (P6)` (기본) \u{007C} `ASCII (P3)`],
  [`Bits` (PPM 전용)], [`8` (기본) \u{007C} `10` \u{007C} `12` \u{007C} `16`],
)

PPM 의 비트 심도 선택은 DDI 검증에서 특히 유용합니다. 10비트 패널 데이터를
`P6 / 10bit` 로 뽑으면 maxval 이 `1023` 인 PPM 이 되어, 8비트로 눌리지 않은 값을
외부 스크립트에서 그대로 읽을 수 있습니다.

#warn[
저장 시 알파 채널은 제외되고 RGB 3채널로 기록됩니다. HDR/float 영상은 `[0, 1]`
범위로 클램프된 뒤 정수화되므로 범위를 벗어난 값은 잘립니다. 또한 diff 모드가
`SSIM` 또는 `FLIP` 인 상태에서 `Diff` 를 저장하면 해당 히트맵이 아니라
*Pixel Absolute* 결과가 저장됩니다. SSIM/FLIP 맵을 그대로 파일로 남기려면
헤드리스 `--diff-out` 을 쓰십시오 (8장 참조).
]

=== 스크린샷과 저장의 차이

#key("Shift+Ctrl+S") 는 "화면 캡처"가 아닙니다. av 는 창을 그대로 캡처하는 별도의
스크린샷 기능을 제공하지 않으며, 저장되는 것은 오버레이·격자·박스·상태바가 제외된
*원본 픽셀 데이터*(또는 그 diff 결과)입니다. 검증 리포트에는 이쪽이 정답입니다 \u{2014}
화면 축소로 리샘플링된 이미지가 아니라 실제 값이 남기 때문입니다.

worst 블록 박스나 커튼 분할 같은 *화면 그대로*의 그림이 필요하면 OS 의 캡처 도구
(macOS #key("Shift+Cmd+4"), Windows #key("Win+Shift+S"))를 쓰거나, 아래의
클립보드 복사로 픽셀 데이터를 가져와 리포트 도구에서 합성하십시오.

=== 패널 우클릭 컨텍스트 메뉴

패널 위에서 오른쪽 버튼을 *0.5초 이상 길게* 눌렀다 떼면 (드래그 없이, #key("Ctrl")
을 누르지 않은 상태) 작은 컨텍스트 메뉴가 뜹니다.

#table(
  columns: (auto, 1fr),
  [*항목*], [*동작*],
  [`Save As...`], [네이티브 저장 다이얼로그. 확장자(`.png`/`.bmp`/`.ppm`)로 포맷 자동 결정],
  [`Copy to Clipboard`], [그 패널의 영상을 PNG 로 클립보드에 복사],
)

짧게 누르거나 드래그하면 컨텍스트 메뉴 대신 우클릭 드래그 줌 영역 선택으로
동작합니다 (2장 참조).

== 클립보드 복사 (2단계 키)

버그 리포트나 메신저에 붙여 넣을 때는 파일을 거치지 않고 바로 클립보드로
복사하는 편이 빠릅니다. av 는 오조작을 막기 위해 *2단계 키*를 씁니다.

#table(
  columns: (auto, 1fr),
  [*키 조합*], [*동작*],
  [#key("Ctrl+C") 또는 #key("Cmd+C")], [복사 모드 진입 (하단에 안내 배너)],
  [\u{2192} #key("1")], [Image A 를 PNG 로 복사],
  [\u{2192} #key("2")], [Image B 를 PNG 로 복사],
  [\u{2192} #key("3")], [현재 diff 모드의 Diff 영상을 PNG 로 복사],
  [#key("Esc")], [복사 모드 취소],
  [5초 경과], [자동 취소 (타임아웃)],
)

#key("Ctrl+C") 를 누르면 화면 하단에 노란 테두리 배너가 뜹니다.

```
Copy:  1 = A    2 = B    3 = Diff    (Esc to cancel)
```

여기서 `1`/`2`/`3` (숫자 키패드 포함)을 누르면 해당 영상이 PNG 로 인코딩되어
시스템 클립보드에 등록되고, 초록 배너로 `Copied: Image A` 가 *1.5초* 표시됩니다.
#key("Esc") 를 누르면 조용히 빠져나옵니다. `1`/`2`/`3` 도 #key("Esc") 도 아닌 다른
키를 누르면 복사 모드가 취소된 뒤 그 키의 원래 기능이 그대로 수행되므로, 모드에
갇히는 일은 없습니다.

#note[
- 복사 데이터는 알파를 포함한 RGBA PNG 이며, MIME 타입은 `image/png` 로 등록됩니다.
- #key("Shift+Space") 로 A/B 를 스왑한 상태에서도 `1` 은 언제나 화면 왼쪽에 보이는
  영상을 복사합니다 (표시 위치 기준).
- `3` (Diff) 은 A·B 가 모두 로드돼 있어야 하며, 저장과 마찬가지로 SSIM/FLIP 모드는
  Pixel Absolute 로 대체됩니다.
]

#warn[
클립보드 데이터는 SDL3 의 콜백 방식으로 등록됩니다. 즉 *av 프로세스가 살아 있는
동안* 붙여넣기 요청에 응답합니다. macOS 와 Windows 에서는 PNG 클립보드를 받는
앱에 곧바로 붙여 넣을 수 있지만, Linux(X11/Wayland)에서는 클립보드 매니저가
없으면 av 를 종료하는 순간 내용이 사라집니다. 원격 세션이나 CI 장비에서는 클립보드
대신 `Save Images` 나 헤드리스 `--diff-out` 을 쓰는 편이 확실합니다.
]

== 실무 시나리오 \u{2014} 100프레임 시퀀스에서 회귀 프레임 찾기

보상 IP 펌웨어를 갱신한 뒤, 100프레임 테스트 패턴 시퀀스에서 화질이 무너진
프레임을 찾아 리포트하는 전형적인 흐름입니다.

#ex("회귀 프레임 추적")[
*1단계 \u{2014} 헤드리스로 범위를 좁힙니다.* 눈으로 100장을 보기 전에 수치로 후보를
고릅니다.

```
av --pair --metrics /work/golden/frame_0001.png /work/hw_out > psnr.csv
sort -t, -k4 -g psnr.csv | head -5
```

`psnr_db` 컬럼이 가장 낮은 다섯 프레임이 나옵니다. 여기서 `frame_0057.png` 가
`52.1 dB` 로 다른 프레임(`> 60 dB`)보다 확연히 낮다고 가정합니다.

*2단계 \u{2014} 해당 프레임을 GUI 로 엽니다.* 프레임 정렬이 깨지지 않도록 반드시
`--pair` 로 엽니다.

```
av --pair /work/golden/frame_0057.png /work/hw_out
```

*3단계 \u{2014} 앞뒤 프레임을 훑습니다.* #key("a") 로 56, 55번을, #key(";") 로 58, 59번을
확인합니다. 두 패널이 항상 같은 파일명으로 함께 움직이므로 프레임이 어긋날 걱정이
없습니다. 상단 토스트로 지금 몇 번인지, 상태바 `A [57/100]` 으로 위치를 확인합니다.

*4단계 \u{2014} 블링크로 차이를 눈에 띄게 만듭니다.* #key(",") 를 누르면 A 와 B 가 한
패널에서 자동 교대되어 미세한 차이가 "깜빡임"으로 드러납니다. 너무 빠르면
#key("<"), 너무 느리면 #key(">") 로 `0.1`초씩 조절합니다 (기본 `0.5`초, 범위
`0.1`\u{007E}`2.0`초). 블링크 중에는 두 패널의 뷰포트가 강제로 동기화되므로
픽셀 단위로 정렬된 상태에서 비교됩니다.

*5단계 \u{2014} 증거를 남깁니다.* #key("Ctrl+3") 으로 Pixel Absolute diff 를 켜고
#key("Ctrl+C") \u{2192} #key("3") 으로 diff 영상을 클립보드에 복사해 이슈에 바로
붙여 넣습니다. 파일로 남겨야 하면 #key("Shift+Ctrl+S") 로 `Save Images` 를 열고
`Diff` 를 체크한 뒤 `Save All Checked` 를 누릅니다.
]

#tip[
결손 프레임 점검도 같은 흐름에 포함됩니다. CSV 의 `missing` 상태 행이 있으면 HW
덤프에서 빠진 프레임이며, GUI 에서 그 프레임으로 이동하면 B 패널에
`No matching image: frame_XXXX.png` 가 표시되어 즉시 확인됩니다.
]
