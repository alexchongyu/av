#import "_common.typ": *

= 두 영상 비교 — Diff 모드

이 장은 av의 핵심 기능인 두 영상(A/B) 비교를 다룹니다. 10가지 Diff 모드가 각각
무엇을 계산하고 어떤 색으로 보여 주는지, 증폭·허용오차 같은 파라미터를 어떻게
조작하는지, 그리고 차이 픽셀을 숫자로 확인하는 Diff 픽셀 리스트 창과 블링크
비교를 순서대로 설명합니다. DDI 보상 IP 검증에서는 대개 A = 원본(reference),
B = 보상 결과(compensated output)로 놓고 씁니다.

== Diff 패널이란

두 영상이 모두 로드된 상태에서 Diff 모드를 켜면 화면이 세 패널로 나뉩니다 \u{2014}
왼쪽 A, 가운데 B, 오른쪽 Diff 입니다. 각 패널 폭은 창 폭의 1/3 이며, 기본 테두리
색은 A = 마젠타, B = 노랑, Diff = 시안입니다(`-bc` 로 변경, 10장 참조).

Diff 패널은 A 패널과 같은 뷰포트를 공유합니다. 즉 A 패널을 확대·이동하면 Diff
패널이 그대로 따라오고, 뷰포트 동기화(`S`)가 켜져 있으면 B 패널까지 세 장이 함께
움직입니다. 차이 계산은 화면에 실제로 그려지는 픽셀에 대해 매 프레임 수행되므로,
확대하면 확대한 만큼 정밀하게 관찰할 수 있습니다.

#fig_diff_pipeline()

Diff 모드가 켜져 있는데 두 영상이 완전히 동일하면 Diff 패널 한가운데에 초록색
`Identical` 글자가 크게 표시됩니다(Alpha Blend 모드는 제외). 채널 보기 모드
(#key("Shift+R") / #key("Shift+G") / #key("Shift+B"))에서는 해당 채널만 같아도
`Identical` 이 뜹니다.

#note[
Diff 패널의 렌더링은 GPU(OpenGL 셰이더)와 소프트웨어 렌더러(`--software`)가
동일한 수식을 사용하도록 구현되어 있습니다. 따라서 GPU 가 없는 CI 머신이나 원격
X11 세션에서도 화면 결과가 같습니다.
]

== Diff 모드 한눈에 보기

모드 전환은 모두 #key("Ctrl") + 숫자 조합이며 *토글* 방식입니다. 같은 키를 다시
누르면 그 모드가 꺼지고 Diff 가 Off 로 돌아갑니다. 메뉴 `Diff` 에서도 같은
목록을 고를 수 있습니다.

#table(
  columns: (auto, auto, auto, 1fr),
  [*키*], [*메뉴 항목*], [*태그 / CLI 토큰*], [*한 줄 요약*],
  [#key("Ctrl+0")], [FLIP],        [`FLIP` / `flip`],             [NVIDIA FLIP-LDR 지각 오차맵 (magma)],
  [#key("Ctrl+1")], [Signed],      [`Signed` / `signed`],         [부호 있는 차이: 파랑 = A\<B, 흰 = 동일, 빨강 = A>B],
  [#key("Ctrl+2")], [Alpha Blend], [`AlphaBlend` / `alphablend`], [A와 B를 알파 비율로 혼합해 겹쳐 보기],
  [#key("Ctrl+3")], [Absolute],    [`Abs` / `abs`],               [채널별 절대차 |A\u{2212}B|],
  [#key("Ctrl+4")], [Relative],    [`Rel` / `rel`],               [A 기준 상대 오차 |A\u{2212}B| / A],
  [#key("Ctrl+5")], [Enhance],     [`Enhance` / `enhance`],       [0이 아닌 차이를 128\~255로 remap],
  [#key("Ctrl+6")], [FalseColor],  [`FalseColor` / `falsecolor`], [오차 크기를 남색\u{2192}빨강 컬러맵으로],
  [#key("Ctrl+7")], [SSIM],        [`SSIM` / `ssim`],             [구조 유사도 히트맵 + 점수],
  [#key("Ctrl+9")], [Highlight],   [`Highlight` / `highlight`],   [다른 픽셀만 빨갛게, 나머지는 검정],
  [(메뉴) Off],      [Off],         [`None` / `none`],             [Diff 끄기 \u{2014} 2패널 A \| B 로 복귀],
)

#warn[
`Diff` 메뉴의 `Off` 항목에는 단축키 라벨이 #key("Ctrl+D") 로 표시되지만, 실제
키보드에서 #key("Ctrl+D") 는 *Diff 픽셀 리스트 창을 여닫는* 키입니다(아래 절
참조). 키보드만으로 Diff 를 끄려면 *현재 켜져 있는 모드의 키를 한 번 더* 누르거나
메뉴 `Diff` \u{2192} `Off` 를 고르십시오. 예: Signed 상태에서 #key("Ctrl+1").
]

#note[
`--comp`(3영상 블록 비교) 모드에서는 Diff 가 매 프레임 강제로 Off 로 유지되므로
#key("Ctrl+0")\~#key("Ctrl+9") 가 아무 효과가 없습니다. 3영상 비교는 5장을
참조하십시오.
]

Diff 모드·증폭·허용오차·블링크 간격은 `~/.av.ini` 에 저장되지 않습니다. 즉 av 를
다시 실행하면 항상 Diff Off, 증폭 1.0, 허용오차 0 에서 시작합니다. 매번 같은
조건으로 열고 싶다면 CLI 옵션(아래 "CLI로 초기 상태 지정")을 쓰십시오.

== 절대 · 상대 · 부호 차이 맵

=== Absolute — 기본 차이 맵 (#key("Ctrl+3"))

채널별로 $|A - B|$ 를 구해 증폭을 곱한 뒤 0\~1로 잘라 그대로 RGB 로 그립니다.
차이가 없으면 검정입니다. R 성분만 다르면 붉게, G 만 다르면 초록으로 보이므로
"어느 채널이 틀어졌는가"를 색으로 바로 읽을 수 있습니다.

CLI 단축 옵션 `-d`(= `--diff`)가 정확히 이 모드입니다.

```
av -d orig.png comp.png
```

#ex("보상 결과의 채널 편중 확인")[
원본과 보상 결과를 `av -d orig.png comp.png` 로 열고 #key("]") 를 여덟 번 눌러
증폭을 5.0 으로 올립니다. Diff 패널 전체가 푸르스름하게 뜬다면 B 채널만 계통적으로
틀어졌다는 뜻이므로, 보상 LUT 의 B 축을 먼저 의심하면 됩니다.
]

=== Relative — 어두운 영역에 민감한 상대 오차 (#key("Ctrl+4"))

$|A - B| / max(A + 0.001, 0.001)$ 를 채널별로 계산합니다(분모의 `0.001` 은
0 나눗셈 방지용 eps). 같은 1 LSB 차이라도 밝은 영역에서는 작게, 어두운 영역에서는
크게 보입니다. 저계조(low gray) 보상 품질, 블랙 레벨 근처의 밴딩·색틀어짐을 볼 때
Absolute 보다 유리합니다.

#tip[
저계조 검증에서는 Relative 로 위치를 먼저 찾고, 그 자리를 32배 이상 확대해
Absolute 나 Enhance 로 실제 LSB 값을 확인하는 순서가 빠릅니다.
]

=== Signed — 오버슛과 언더슛을 한 화면에 (#key("Ctrl+1"))

Absolute 계열이 절대값이라 "어느 쪽이 밝은지"를 버리는 반면, Signed 는 부호를
살립니다. Rec.709 휘도로 $0.2126 dot Delta R + 0.7152 dot Delta G + 0.0722 dot Delta B$
를 구해 증폭을 곱하고 \u{2212}1\~+1 로 자른 뒤, 파랑\u{2014}흰색\u{2014}빨강
발산형(diverging) 컬러맵에 태웁니다.

#table(
  columns: (auto, 1fr),
  [*색*], [*의미*],
  [빨강], [A > B \u{2014} A 가 더 밝다 (보상 결과가 어두워짐 = 언더슛)],
  [흰색], [A = B \u{2014} 차이 없음],
  [파랑], [A \< B \u{2014} A 가 더 어둡다 (보상 결과가 밝아짐 = 오버슛)],
)

엣지 주변에서 빨강 띠와 파랑 띠가 번갈아 나타나면 전형적인 링잉(ringing)입니다.
샤프닝·디링잉 IP, ODC(over-drive) 계수 검증에 가장 먼저 켜는 모드입니다.

```
av --diff-mode signed --amplify 8 orig.png comp.png
```

=== Highlight — 다른 픽셀만 빨갛게 (#key("Ctrl+9"))

세 채널 차이 중 최대값에 증폭을 곱해 0보다 크면 빨강(밝기 128\~255), 0이면
검정으로 칠합니다. 차이의 *크기*보다 *위치*가 궁금할 때 씁니다.

이 모드에서는 상태바에 `[Diff: 1234 / 2073600 px]` 형식으로 현재 화면에 그려진
픽셀 중 차이가 있는 픽셀 수 / 전체 픽셀 수가 함께 표시됩니다. 카운트는 화면
뷰포트 기준이므로, 영상 전체 통계를 원하면 먼저 #key("F") (fit)로 전체를 띄우고
읽으십시오.

== 컬러맵 계열

#fig_diff_colormaps()

=== False Color — 오차 크기를 색으로 (#key("Ctrl+6"))

세 채널 차이의 평균(단일 채널 모드에서는 해당 채널 값)에 증폭을 곱한 값을
0\~1 로 정규화해 컬러맵에 태웁니다. 색 순서는 짙은 남색 \u{2192} 파랑 \u{2192}
초록 \u{2192} 노랑 \u{2192} 빨강이며, 구간 경계는 0.25 / 0.50 / 0.75 입니다.

#table(
  columns: (auto, 1fr),
  [*색*], [*정규화 오차 (증폭 반영 후)*],
  [짙은 남색 \u{2192} 파랑], [0.00 \~ 0.25],
  [파랑 \u{2192} 초록],      [0.25 \~ 0.50],
  [초록 \u{2192} 노랑],      [0.50 \~ 0.75],
  [노랑 \u{2192} 빨강],      [0.75 \~ 1.00],
)

리포트용 그림에 쓰기 좋습니다. 증폭을 바꾸면 색 스케일도 함께 바뀌므로, 여러 장을
비교할 때는 반드시 같은 증폭값으로 캡처하십시오.

=== Enhance — 1 LSB 차이도 보이게 (#key("Ctrl+5"))

이 모드만은 화면 전체가 아니라 *두 영상 전체*를 먼저 훑어 0이 아닌 차이값의 전역
최소·최대(0\~255 스케일)를 구합니다. 그리고 그 `[min, max]` 구간을
`[128, 255]` 로 선형 remap 합니다. 차이가 0인 픽셀만 검정으로 남습니다.

결과적으로 "차이가 있는 곳은 무조건 중간 회색 이상"이 되므로, 1 LSB 밖에 차이가
없는 영역도 놓치지 않습니다. Demura 잔차, 디더링 패턴 차이처럼 진폭이 아주 작은
결함을 찾을 때 가장 강력합니다.

#warn[
Enhance 는 스케일이 데이터에 따라 자동으로 바뀌므로 *두 캡처 간 밝기를 서로 비교할
수 없습니다*. "얼마나 다른가"는 Absolute/Signed 나 상태바 PSNR 로 확인하고,
Enhance 는 "어디가 다른가"에만 쓰십시오.
]

== 지각 · 구조 메트릭 히트맵

SSIM 과 FLIP 은 픽셀 단위 뺄셈이 아니라 사람이 느끼는 차이를 모델링한
메트릭입니다. 두 모드 모두 배경 스레드에서 전체 영상을 한 번 계산해 히트맵
텍스처를 만들고, 그 텍스처를 Diff 패널에 표시합니다. 계산 중에는 상태바에
`SSIM: computing…` / `FLIP: computing…` 이 뜹니다. 영상을 다시 로드하거나 시퀀스를
넘기면 자동으로 재계산됩니다.

=== SSIM — 구조 유사도 (#key("Ctrl+7"))

11 $times$ 11 가우시안 윈도우($sigma$ = 1.5), $C_1 = (0.01 dot 255)^2$,
$C_2 = (0.03 dot 255)^2$ 의 표준 SSIM 을 픽셀마다 구한 뒤,
비유사도 $(1 - "SSIM") / 2$ 를 False Color 와 같은 컬러맵으로 그립니다. 파랑에
가까울수록 구조가 같고, 빨강에 가까울수록 구조가 깨진 곳입니다.

상태바에는 전체 평균 점수가 `SSIM: 0.9987` 형식(소수 4자리)으로 표시되며 색이
품질 신호를 겸합니다.

#table(
  columns: (auto, 1fr),
  [*상태바 색*], [*SSIM 점수*],
  [초록], [0.99 초과],
  [노랑], [0.90 초과 \u{2264} 0.99],
  [빨강], [0.90 이하],
)

=== FLIP — 지각 오차맵 (#key("Ctrl+0"))

NVIDIA 의 FLIP-LDR(Andersson et al., 2020)을 충실히 이식한 구현입니다.
sRGB \u{2192} YCxCz 변환, 대비 감도 함수(CSF) 공간 필터링, Hunt 보정 CIELab
공간의 HyAB 색차, 엣지·포인트 특징 검출을 결합해 픽셀마다 0\~1 의 지각 오차를
내고 magma 컬러맵(검정 \u{2192} 보라 \u{2192} 주황 \u{2192} 연노랑)으로 그립니다.
검정이 "차이 없음", 밝을수록 "사람 눈에 잘 보이는 차이"입니다.

기본 ppd(pixels-per-degree)는 67.0206 으로, 폭 0.7 m 인 4K 모니터를 0.7 m 거리에서
보는 조건에 해당합니다. 화면 평균이 점수이며 *낮을수록 좋습니다*(0 = 동일).

#table(
  columns: (auto, 1fr),
  [*상태바 색*], [*FLIP 점수 (낮을수록 좋음)*],
  [초록], [0.05 미만],
  [노랑], [0.05 이상 0.15 미만],
  [빨강], [0.15 이상],
)

#note[
av 의 FLIP 구현은 NVIDIA 공식 `flip` 레퍼런스 툴과 *소수 5자리까지 일치*하는 것이
검증되어 있습니다. 따라서 사내 리포트의 FLIP 수치를 레퍼런스 값과 그대로 비교해도
됩니다. 헤드리스 `--metrics` 의 FLIP 열도 같은 엔진을 씁니다(8장 참조).
]

#tip[
PSNR 이 40 dB 를 넘어 "수치상 문제 없음"인데도 눈으로 보면 거슬리는 경우가
있습니다. 이럴 때 FLIP 을 켜면 사람이 실제로 인지하는 영역만 밝게 뜨므로,
"수치는 좋은데 왜 이상한가"를 설명하는 근거 그림으로 쓰기 좋습니다.
]

== Alpha Blend — 겹쳐 보기 (#key("Ctrl+2"))

유일하게 차이를 계산하지 않는 모드입니다. Diff 패널에 $(1 - alpha) dot A + alpha dot B$
를 그립니다. `alpha` 기본값은 0.5(A 50% + B 50%)이고 범위는 0.0\~1.0 입니다
(0 = A 만, 1 = B 만). 패널 좌상단에 `A: 50%  —  B: 50%` HUD 가 표시됩니다.

이 모드에서만 #key("[") / #key("]") 와 #key("\\") 의 동작이 달라집니다.

#table(
  columns: (auto, 1fr),
  [*키*], [*Alpha Blend 에서의 동작*],
  [#key("[") / #key("]")],             [alpha \u{2212}1% / +1%],
  [#key("Shift+[") / #key("Shift+]")], [alpha \u{2212}10% / +10%],
  [#key("\\")],                        [alpha = 50% 로 리셋],
)

허용오차(threshold)는 Alpha Blend 에는 적용되지 않으며, `Identical` 오버레이도
뜨지 않습니다. B 영상이 로드되지 않은 상태에서 #key("Ctrl+2") 를 누르면
`B image required for Alpha Blend` 토스트가 약 1.8초 동안 표시됩니다.

#ex("정렬(alignment) 확인")[
패턴 영상 두 장의 위치가 1픽셀 어긋났는지 보려면 #key("Ctrl+2") 로 Alpha Blend 를
켜고 #key("[") 와 #key("]") 를 번갈아 눌러 alpha 를 0%와 100% 사이에서 흔들어
보십시오. 엣지가 좌우로 떨리면 정렬이 어긋난 것이고, 제자리에서 밝기만 변하면
정렬은 맞고 계조만 다른 것입니다.
]

== 파라미터 조작: 증폭 · 알파 · 허용오차

=== 증폭 (amplify)

차이가 너무 작아 검게만 보일 때 증폭을 올립니다. 계산된 차이에 곱해지는 배율이며,
Signed / Absolute / Relative / Highlight / False Color / Enhance 에 영향을 줍니다
(SSIM · FLIP 히트맵과 Alpha Blend 는 영향 없음).

#table(
  columns: (auto, auto, 1fr),
  [*조작*], [*변화량*], [*비고*],
  [#key("[") / #key("]")], [\u{2212}0.5 / +0.5], [범위 0.5 \~ 100.0],
  [#key("\\")],            [1.0 으로 리셋],       [Alpha Blend 에서는 alpha = 50%],
  [메뉴 `Diff` \u{2192} Amplify 슬라이더], [연속 조절], [슬라이더 범위 0.5 \~ 50.0],
  [`--amplify <val>`],     [시작값 지정],         [기본 1.0, 도움말 표기 범위 0.1 \~ 100],
)

현재 값은 상태바에 `Diff: Signed (A-B)  x8.0` 처럼 표시됩니다. 괄호 안의
`(A-B)` 는 비교 방향이며, #key("Shift+Space") 로 A/B 를 맞바꾸면 `(B-A)` 로
바뀝니다.

#warn[
`--amplify` 로 준 값은 클램프되지 않습니다. `--amplify 0.2` 처럼 0.5 미만을 주면
그 값으로 시작하지만, 이후 #key("[") 를 누르는 순간 하한 0.5 로 올라붙습니다.
재현 가능한 캡처가 필요하면 0.5 이상의 값을 쓰십시오.
]

=== 허용오차(threshold)와 Tolerance diff

허용오차는 "이 값 이하의 차이는 무시"라는 기준입니다. 기본값 0 은 비활성이고,
1\~255 범위(0\~255 스케일 기준)로 설정합니다. 켜면 채널 최대 차이가 threshold
이하인 픽셀이 어두운 회색(40, 40, 40)으로 눌리고, 초과한 픽셀만 원래 Diff 색으로
남습니다.

#table(
  columns: (auto, 1fr),
  [*키*], [*동작*],
  [#key("Ctrl+8")],                     [Tolerance diff 토글 \u{2014} 0이면 1로, 그 외에는 0으로],
  [#key("Shift+[") / #key("Shift+]")],  [threshold \u{2212}1 / +1 (Alpha Blend 에서는 alpha \u{2213}10%)],
  [#key("Ctrl+\\")],                    [threshold = 0 (리셋)],
)

threshold 가 1 이상이면 상태바에 `Thr: 3 (0.8% exceed)` 가 표시됩니다. 괄호 안은
현재 화면에 그려진 픽셀 중 임계를 초과한 비율입니다.

#ex("규격 1 LSB 이내 판정")[
"보상 결과는 원본 대비 2 LSB 이내"라는 규격을 확인한다고 합시다.
#key("Ctrl+3") 으로 Absolute 를 켜고 #key("Ctrl+8") 로 Tolerance 를 켠 뒤
#key("Shift+]") 를 한 번 더 눌러 `Thr: 2` 로 맞춥니다. 화면이 전부 어두운
회색이면 규격 통과이고, 남아 있는 색점이 규격 위반 픽셀입니다. 상태바의
`(x.x% exceed)` 를 그대로 리포트 수치로 쓸 수 있습니다.
]

=== `--comp` 모드에서의 대괄호 키

#warn[
`--comp`(3영상 블록 비교) 모드에서는 #key("[") / #key("]") 가 증폭 조절이 아니라
*worst 블록 순회*로 동작합니다(#key("Shift+[") / #key("Shift+]") 도 마찬가지).
자세한 내용은 5장을 참조하십시오.
]

=== CLI로 초기 상태 지정

```
av --diff-mode <mode> [--amplify <val>] A B
```

`<mode>` 에 넣을 수 있는 토큰은 `none`, `alphablend`, `abs`, `rel`, `highlight`,
`falsecolor`, `ssim`, `flip`, `signed`, `enhance` 이며 기본값은 `none` 입니다.
잘못된 토큰을 주면 `Unknown diff-mode: ...` 를 출력하고 종료합니다.

```
# Signed + 증폭 8배로 바로 시작 (링잉 검사 프리셋)
av --diff-mode signed --amplify 8 orig.png comp.png

# 절대차 단축 옵션 (-d 는 --diff-mode abs 와 동일)
av -d orig.png comp.png

# FLIP 지각 오차맵을 전체 맞춤 배율로 열기
av --diff-mode flip --zoom fit ref.png test.png
```

차이 맵을 파일로 뽑는 헤드리스 `--diff-out` / `--sbs` 는 8장에서 다룹니다.

== Diff 픽셀 리스트 창 (#key("Ctrl+D"))

차이가 있는 픽셀을 *숫자 표*로 확인하는 창입니다. Diff 모드가 켜져 있는 상태에서
#key("Ctrl+D") 를 누르면 화면 상단 가운데에 `Diff Pixels` 창이 열립니다.

#table(
  columns: (auto, 1fr),
  [*열*], [*내용*],
  [`#`],      [차이 픽셀 일련번호 (1부터)],
  [`Pos`],    [이미지 좌표 `(x,y)`],
  [`A(RGB)`], [A 영상의 픽셀값 `r,g,b`],
  [`B(RGB)`], [B 영상의 픽셀값 `r,g,b`],
  [`D(RGB)`], [절대차 `|A-B|` (붉은 글씨)],
)

- *정렬 순서*: 별도의 정렬 기능은 없고, 영상을 위에서 아래로 \u{2192} 왼쪽에서
  오른쪽으로 훑은 래스터 순서 그대로입니다.
- *페이지*: 한 번에 50개씩 표시하며, 창 폭에 따라 같은 5열 묶음이 최대 4벌까지
  가로로 반복 배치됩니다. 헤더에
  `Showing #1 ~ #50 / 8123 different  (2073600 total pixels)` 형식으로 현재 구간과
  전체 차이 픽셀 수, 비교 대상 총 픽셀 수가 표시됩니다.
- *이동*: 아래쪽 `< Prev` / `Next >` 버튼으로 50개 단위 이동, `List from #` 에
  번호를 넣고 Enter 를 치면 그 번호부터 목록을 다시 그립니다.
- *클릭 동작*: `Go to pixel #` 에 번호를 넣고 `Go` 버튼(또는 Enter)을 누르면 A/B
  두 뷰포트가 모두 *32배 줌*으로 바뀌면서 해당 픽셀이 화면 정중앙에 오도록
  이동합니다. 목록도 그 픽셀부터 다시 시작합니다.
- *닫기*: 창 오른쪽 아래 `Close`, 창 제목줄의 X, #key("Esc"), 또는 #key("Ctrl+D")
  를 다시 누르면 닫힙니다.

값의 단위는 원본 비트심도를 따릅니다. PPM(P2/P3/P5/P6) 10비트 영상이면
0\~1023 으로 표시되고, 일반 8비트 영상은 0\~255 입니다. HDR/float 영상은 0\~255
스케일로 환산된 정수로 나열됩니다.

채널 보기 모드에서는 선택한 채널의 차이가 0이 아닌 픽셀만 남기고, `A`/`B`/`D` 열도
`A(R)`, `B(R)`, `D(R)` 처럼 단일 값으로 바뀝니다.

#warn[
이 창은 Diff 패널이 한 번이라도 그려져 차이 목록이 계산된 뒤에만 내용을 표시합니다.
Diff 모드가 Off 인 상태에서 #key("Ctrl+D") 를 눌러도 아무것도 뜨지 않습니다.
또 두 영상이 완전히 동일하면(선택 채널 기준으로 동일해도) 창이 스스로 닫힙니다 \u{2014}
`Identical` 오버레이가 그 답입니다.
]

#note[
`--comp` 모드에서 #key("Ctrl+D") 는 diff 픽셀 리스트가 아니라 *worst 블록 리스트
창*을 토글합니다(행을 클릭하면 세 패널이 동시에 그 블록으로 점프). 5장을
참조하십시오.
]

=== 화면에서 직접 숫자 읽기

Diff 패널을 32배 이상 확대하면 픽셀마다 격자선과 함께 차이값이 직접 그려집니다.
RGB 모드에서는 빨강·초록·파랑 세 줄로 세로로 쌓이고, 단일 채널 모드에서는 한 줄만
나옵니다. 표시 형식은 #key("Ctrl+X") 로 10진수 / `0x` 16진수 / `h` 접미 16진수를
순환할 수 있습니다(3장 참조).

32배 미만에서는 마우스를 올린 자리에 돋보기가 뜨고, 그 아래에
`diff: 3, 0, 12` 형식으로 중심 픽셀의 채널별 절대차가 표시됩니다. 돋보기는
#key("Ctrl+M") 으로 끄고 켤 수 있으며 32배 이상에서는 자동으로 숨습니다.

== 블링크 비교 (#key(","))

두 영상을 번갈아 깜빡여 눈으로 차이를 잡는 고전적인 기법입니다. 두 영상이 모두
로드된 상태에서 #key(",") 를 누르면 화면이 *한 패널 전체*로 바뀌고 A 와 B 가
자동으로 교대합니다.

#table(
  columns: (auto, 1fr),
  [*키*], [*동작*],
  [#key(",")],                 [블링크 시작 / 종료],
  [#key("<") (= #key("Shift+,"))], [교대 간격 \u{2212}0.1초],
  [#key(">") (= #key("Shift+."))], [교대 간격 +0.1초],
  [#key("Esc")],               [블링크 종료],
)

간격의 기본값은 0.5초, 범위는 0.1\~2.0초, 조절 단위는 0.1초입니다. 상태바에는
작은 원형 표시와 함께 `BLINK A  0.50s` 형식으로 현재 위상(A 또는 B)과 간격이
표시되고, 패널 테두리 색도 위상에 따라 A 색(마젠타) \u{2194} B 색(노랑)으로
바뀌므로 지금 어느 쪽을 보고 있는지 헷갈리지 않습니다.

블링크를 켜면 활성 패널의 뷰포트가 양쪽에 복사되고 뷰포트 동기화가 강제로
켜집니다(두 영상이 정확히 같은 위치에서 깜빡여야 하기 때문). 블링크를 끄면 이전
동기화 설정이 그대로 복원됩니다.

#tip[
확대율이 낮으면 블링크의 효과가 약합니다. 의심 영역을 8\~16배로 확대한 뒤 간격을
#key("<") 로 0.2초까지 줄이면 1 LSB 밴딩 경계도 깜빡임으로 드러납니다.
]

`--comp` 모드에서 #key(",") 는 원본 \u{2192} img2 \u{2192} img3 를 한 패널에서
순환하는 3-way 블링크가 됩니다(5장 참조).

== 상태바와 Info 창 판독

=== 상태바

#key("U") 로 UI 를 켜면 창 아래쪽 상태바에 Diff 관련 정보가 왼쪽부터 차례로
나타납니다. 표시 조건이 안 맞는 항목은 아예 나오지 않습니다.

#table(
  columns: (auto, 1fr),
  [*표시*], [*의미와 조건*],
  [`Diff: Signed (A-B)  x8.0`], [활성 모드 태그, 비교 방향, 증폭. Diff 가 켜져 있을 때],
  [`SSIM: 0.9987`],             [SSIM 모드일 때 평균 점수 (계산 중이면 `computing…`)],
  [`FLIP: 0.0123`],             [FLIP 모드일 때 평균 점수 (낮을수록 좋음)],
  [`PSNR: 42.3 dB`],            [Diff 가 켜져 있으면 자동 계산. 완전 동일하면 `PSNR: inf`],
  [`[Diff: 1234 / 2073600 px]`],[Highlight 모드의 차이 픽셀 수 / 화면 픽셀 수],
  [`Thr: 2 (0.8% exceed)`],     [허용오차가 1 이상일 때 임계값과 초과 비율],
  [`BLINK A  0.50s`],         [블링크 중일 때 현재 위상과 간격],
)

상태바 PSNR 은 R/G/B 채널별 PSNR 의 평균이며, 색이 곧 신호등입니다 \u{2014}
40 dB 초과는 초록, 30 dB 초과는 노랑, 30 dB 이하는 빨강입니다. Diff 를 끄면 PSNR
표시도 사라집니다.

=== Info 창 (#key("I"))

#key("I") 로 여는 `Image Info` 창에는 A/B 의 경로·해상도·채널 수·HDR 여부, 각
패널의 줌과 팬 값, 그리고 PSNR 한 줄이 표시됩니다. Diff 모드와 무관하게 열리는
즉시 계산됩니다.

#table(
  columns: (auto, 1fr),
  [*표시*], [*의미*],
  [`PSNR: 42.31 dB   [A(Left)=ref, B(Right)=cmp]`], [정상 계산. A 가 기준, B 가 비교 대상],
  [`PSNR: inf  (identical)`],                       [두 영상이 완전히 동일],
  [`PSNR: N/A (size/format mismatch)`],             [해상도 또는 픽셀 포맷이 달라 계산 불가],
)

색 규칙은 상태바와 같습니다(40 dB 초과 초록 / 30 dB 초과 노랑 / 그 이하 빨강).
`--comp` 모드에서는 이 자리에 img2, img3 각각의 원본 대비 PSNR 두 줄이 표시됩니다
(5장 참조).

== 크기가 다른 두 영상

해상도가 다른 두 장을 열어도 av 는 거부하지 않고 *겹치는 좌상단 영역*
$min(W_A, W_B) times min(H_A, H_B)$ 만 비교합니다. 다만 표시 지점마다 처리가
다르므로 아래를 기억해 두십시오.

#table(
  columns: (auto, 1fr),
  [*기능*], [*크기가 다를 때의 동작*],
  [Diff 패널],        [A 의 크기를 기준으로 뷰포트를 잡고, 겹치는 영역만 차이를 그림],
  [`Identical` 오버레이], [겹치는 영역이 같으면 표시됨 (전체가 같다는 뜻이 아님)],
  [Diff 픽셀 리스트], [겹치는 영역만 나열. 각 영상의 실제 행 폭으로 인덱싱하므로 좌표는 정확],
  [Info 창 PSNR],     [`N/A (size/format mismatch)` \u{2014} 계산하지 않음],
  [`--diff-out`],     [`[diff-out] size mismatch: ...` 출력 후 exit 5],
  [`--metrics`],      [해당 행의 상태가 `mismatch` 로 기록됨],
)

#warn[
*상태바의 PSNR 은 크기가 다른 두 영상에서 신뢰할 수 없습니다.* Info 창은 크기
불일치를 명시적으로 `N/A` 로 처리하지만, 상태바 PSNR 은 두 버퍼를 선형 인덱스로
$min(W_A dot H_A, W_B dot H_B)$ 개까지 훑는 방식이라 폭이 다르면 행이 어긋난 채
계산됩니다. 해상도가 다르면 반드시 Info 창(#key("I")) 값을 기준으로 판단하고,
정량 수치가 필요하면 두 영상을 같은 해상도로 맞춘 뒤 헤드리스 `--metrics` 로
뽑으십시오(8장 참조).
]

== 실무 시나리오: 보상 IP 링잉 확인

DDI 보상 IP 의 출력에서 엣지 링잉을 찾아 리포트까지 만드는 전형적인 흐름입니다.

#ex("보상 전/후 링잉 검사 \u{2014} Signed + 증폭 8")[
+ *열기* \u{2014} 원본과 보상 결과를 Signed 모드로 바로 엽니다.

  ```
  av --diff-mode signed --amplify 8 orig.png comp.png
  ```

+ *전체 조망* \u{2014} #key("U") 로 상태바를 켜고 #key("F") 로 전체를 맞춘 뒤
  상태바의 `PSNR` 값을 먼저 기록합니다. 예: `PSNR: 38.6 dB` (노랑 = 주의).

+ *링잉 위치 찾기* \u{2014} 화면에서 엣지를 따라 빨강 띠와 파랑 띠가 교대로 나타나는
  곳을 찾습니다. 빨강은 A > B(보상 결과가 어두워짐 = 언더슛), 파랑은
  A \< B(보상 결과가 밝아짐 = 오버슛)입니다.

+ *증폭 미세 조정* \u{2014} 띠가 포화되어 뭉개져 보이면 #key("[") 로 증폭을 낮추고,
  너무 흐리면 #key("]") 로 올립니다. 원위치는 #key("\\") 입니다.

+ *정량 확인* \u{2014} #key("Ctrl+D") 로 Diff 픽셀 리스트를 열고 `Go to pixel #` 에
  번호를 넣어 `Go` 를 누르면 32배 줌으로 해당 픽셀에 붙습니다. 화면에 직접 찍히는
  채널별 차이값으로 링잉의 진폭(LSB)을 읽습니다.

+ *규격 판정* \u{2014} #key("Ctrl+8") 로 Tolerance 를 켜고 #key("Shift+]") 로
  규격 LSB 까지 올린 뒤, 상태바 `Thr: N (x.x% exceed)` 의 초과 비율을 기록합니다.

+ *지각 관점 교차 확인* \u{2014} #key("Ctrl+0") 으로 FLIP 을 켜 실제로 사람 눈에
  보이는 수준인지 확인합니다. `FLIP: 0.04`(초록)라면 수치상 링잉이 있어도 시감
  영향은 작다고 보고할 수 있습니다.

+ *증거 캡처* \u{2014} #key("Ctrl+C") 를 누른 뒤 #key("3") 을 누르면 Diff 이미지가
  PNG 로 클립보드에 복사됩니다. 파일로 남기려면 #key("Shift+Ctrl+S") 저장
  대화상자를 쓰거나(7장 참조), CI 에서는 헤드리스로 뽑습니다(8장 참조).

  ```
  av --diff-out ringing_sbs.png --diff-mode signed --amplify 8 --sbs orig.png comp.png
  ```
]

#tip[
같은 검사를 프레임 시퀀스 전체에 반복해야 한다면 GUI 로 한 프레임을 확인해 조건을
정한 다음, 그 조건 그대로 `--pair` + `--metrics` + `--fail-psnr` 게이트로 자동화하는
것이 정석입니다. 시퀀스는 7장, CI 연동은 8장을 참조하십시오.
]
