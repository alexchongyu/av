#import "_common.typ": *

= 픽셀 검사와 색 판독

이 장은 av 의 이름(Advanced Pixel *Lens*)이 가리키는 핵심 기능, 즉 "화면에 보이는
그림"이 아니라 "그 아래 깔린 숫자"를 읽는 방법을 다룹니다. 커서 아래 픽셀의
좌표와 값을 읽는 풍선말, 줌 32배 이상에서 자동으로 켜지는 픽셀 그리드와 값
오버레이, 10\/12\-bit 원본값 보존 규칙, 채널 분리 보기, CIE 색도 판독, 그리고
float\/HDR 영상의 결함 픽셀 검출까지를 순서대로 설명합니다. DDI 보상 IP 의
출력이 원본과 LSB 몇 단계나 어긋났는지, 그 어긋남이 색도상 몇 Δu\u{2032}v\u{2032}
인지를 확인하는 것이 이 장의 실무 목표입니다.

== 픽셀 값 풍선말 \u{2014} #key("V")

#key("V") 를 누르면 마우스 커서 아래 픽셀의 좌표와 값을 보여 주는 풍선말이
켜집니다. 메뉴 `View > Show Pixel Info` 로도 같은 토글을 조작할 수 있습니다.
이 상태는 `$HOME/.av.ini` 의 `show_pixel_info=0|1` 로 영속화되므로, 한 번 켜
두면 다음 실행에서도 그대로 켜진 채 시작합니다.

풍선말은 두 줄로 구성됩니다.

#table(
  columns: (auto, 1fr),
  [*줄*], [*내용*],
  [1행], [이미지 좌표 `(x, y)` \u{2014} 회색 글자. 좌상단이 `(0, 0)`],
  [2행], [`R:` `G:` `B:` 값 \u{2014} 각각 빨강\/초록\/파랑 글자색],
)

풍선말은 커서 기준 오른쪽 위(`+16, -40` px)에 뜨고, 화면 오른쪽이나 위쪽
경계에 닿으면 자동으로 반대편으로 접힙니다. 글꼴은 전용 대형 폰트(26 px)라서
멀리서도 값을 읽을 수 있습니다.

=== A\/B 동시 표시 규칙

값 자체는 *커서가 올라가 있는 패널 하나만* 표시합니다. 두 영상을 나란히 띄운
상태에서 왼쪽 패널 위에 커서를 두면 A 값이, 오른쪽으로 옮기면 같은 좌표의 B
값이 나옵니다. 뷰포트 동기화(#key("S"))가 켜져 있으면 두 패널의 같은 화면
위치가 같은 이미지 좌표를 가리키므로, 커서를 좌우로 왕복시키는 것만으로 A\/B
값을 즉시 대조할 수 있습니다.

#table(
  columns: (auto, 1fr),
  [*커서 위치*], [*풍선말이 보여 주는 값*],
  [A 패널(왼쪽)], [A 영상의 해당 픽셀 값],
  [B 패널(오른쪽)], [B 영상의 해당 픽셀 값],
  [Diff 패널(3분할 오른쪽)], [`|A - B|` 채널별 절댓값],
  [`--comp` 세 번째 패널], [img3(`comp.img_c`)의 값],
  [`--comp` 3\-way 블링크 중], [현재 블링크 단계의 영상 값(orig\/img2\/img3)],
)

#note[
  #key("Shift+Space") 로 A\/B 를 스왑한 상태에서도 풍선말은 "지금 그 패널에
  실제로 그려지고 있는 영상"의 값을 읽습니다. 화면과 숫자가 어긋나지 않습니다.
]

=== diff 모드일 때의 표시

diff 모드(4장 참조)에서는 화면이 A \| B \| Diff 3분할이 되고, Diff 패널 위의
풍선말은 두 영상의 *채널별 절댓값 차이* `|A - B|` 를 표시합니다. 이때 값 계산은
아래 "픽셀 값 우선순위"를 그대로 따르므로, 16\-bit PNM 쌍이라면 8\-bit 로 눌린
값이 아니라 원본 스케일의 차이가 나옵니다. 예를 들어 10\-bit(maxval 1023) 원본
쌍에서 `R:3 G:0 B:1` 이면 R 채널이 3 LSB 어긋났다는 뜻입니다.

#warn[
  Diff 패널 위에서는 #key("Shift+V") 컬러메트리 줄이 나오지 않습니다. 색도 판독은
  A 또는 B 패널 위에서만 동작합니다(뒤의 "컬러메트리 풍선말" 절 참조).
]

#ex("보상 IP 출력의 LSB 오차 확인")[
  ```
  av orig_10bit.ppm comp_10bit.ppm
  ```
  1. #key("V") 로 풍선말을 켭니다.
  2. #key("5") 로 32배 줌(2^5)까지 확대하고 관심 영역으로 팬합니다.
  3. 커서를 왼쪽 패널의 의심 픽셀에 올리면 `(412, 77)` / `R:640 G:640 B:641`.
  4. 커서를 오른쪽 패널의 같은 화면 위치로 옮기면 `R:637 G:640 B:641`.
  5. #key("Ctrl+3") 으로 diff 모드에 들어가 Diff 패널에 커서를 두면
     `R:3 G:0 B:0` \u{2014} R 채널만 3 LSB 낮게 보상되었음이 확정됩니다.
]

== 픽셀 값 우선순위와 원본값 보존

av 는 한 픽셀에 대해 최대 세 종류의 버퍼를 가질 수 있고, 값을 표시할 때는 항상
같은 우선순위로 고릅니다. 이 규칙은 풍선말, 줌 오버레이, diff 값 표시가 모두
공유합니다.

#fig_pixel_priority()

#table(
  columns: (auto, auto, 1fr),
  [*순위*], [*버퍼*], [*표시 형식과 조건*],
  [1], [`pixels_orig`], [PNM 원본값(uint16, RGB 3채널). `ppm_maxval > 0` 이고
       버퍼가 비어 있지 않을 때. 범위 `0`\~`maxval` (8\-bit `255`, 10\-bit `1023`,
       12\-bit `4095`, 16\-bit `65535`). #key("Ctrl+X") 형식 순환 대상],
  [2], [`pixels_f32`], [HDR float. 풍선말은 `%.3f`, 줌 오버레이는 `%.2f`.
       음수나 1 초과 값도 그대로 표시하며, 16진 형식 순환 대상이 아님],
  [3], [`pixels`], [LDR uint8 RGBA8. 범위 `0`\~`255`.
       #key("Ctrl+X") 형식 순환 대상],
)

=== 왜 이 순서인가

로더는 PNM(P2\/P3\/P5\/P6)을 *자체 파서*로 읽어 원본 정수값을 `pixels_orig` 에
그대로 보존한 뒤, 화면 표시용 8\-bit 텍스처를 `value * 255 / maxval` 로 따로
만듭니다. 즉 10\-bit 값 `641` 은 화면에는 `159` 로 그려지지만 메모리에는 `641`
이 남아 있습니다. 만약 우선순위가 표시용 버퍼부터였다면 10\-bit 원본에서 1 LSB
차이가 8\-bit 로 눌리면서 사라져 버립니다. 그래서 원본값이 있으면 무조건
원본값을 먼저 씁니다.

HDR 을 두 번째에 두는 이유는 float 값에 정수 표기\(16진 포함\)를 강제하면
정보가 손실되기 때문이고, uint8 을 마지막에 두는 이유는 그것이 항상 존재하는
최후의 fallback 이기 때문입니다.

#tip[
  DDI 보상 IP 검증에서는 톤 곡선\/감마 보상이 10\~12\-bit 도메인에서 일어납니다.
  중간 산출물을 8\-bit PNG 로 덤프하면 검증하려는 오차 자체가 반올림에 묻히므로,
  파이프라인 중간 덤프는 P6 16\-bit PNM(또는 maxval 을 실제 비트폭에 맞춘 PNM)
  으로 남기는 것을 권장합니다. av 는 그 값을 그대로 읽어 줍니다.
]

#warn[
  `pixels_f32` 는 HDR(Radiance) 영상과 `maxval > 255` 인 PNM 에만 만들어집니다.
  PNM 의 float 버퍼는 `원본값 / maxval` 로 정규화되어 항상 `0`\~`1` 안에 들어가므로,
  뒤에 나오는 결함 픽셀 오버레이(#key("/")) 에서는 아무것도 검출되지 않습니다.
  이는 정상 동작입니다.
]

== 줌 32배 이상: 자동 픽셀 그리드와 값 오버레이

확대 배율에 따라 두 단계의 보조 표시가 자동으로 켜집니다. 별도 토글 키는
없습니다.

#table(
  columns: (auto, 1fr),
  [*줌 임계값*], [*자동으로 켜지는 것*],
  [`zoom >= 16`], [셰이더가 그리는 은은한 회색 픽셀 경계선. 픽셀 경계를 40 % 세기의
    중간 회색으로 섞어 그립니다(소프트웨어 렌더러도 동일)],
  [`zoom >= 32`], [흰색 픽셀 격자선 + *셀 중앙에 R\/G\/B 값 3줄*. 매그니파이어는
    이 배율부터 자동으로 숨습니다(픽셀이 이미 충분히 큼)],
)

값 글자 크기는 `min(zoom * 0.25, 20)` px 이고, 계산 결과가 6 px 미만이면 값은
그리지 않습니다. 즉 32배에서는 8 px, 80배 이상에서는 20 px 로 고정됩니다.
글자색은 R 이 빨강, G 가 초록, B 가 파랑이며 1 px 검은 그림자가 깔려 밝은
배경에서도 읽힙니다.

채널 모드(다음 절)가 RGB 가 아니면 3줄 대신 해당 채널 한 줄만 그립니다.
Diff 패널에서도 같은 격자와 값이 나오며, 값은 `|A - B|` 입니다.

#ex("32배 확대에서 블록 경계 아티팩트 읽기")[
  ```
  av --zoom 32 orig.ppm comp.ppm
  ```
  창이 열리자마자 32배 상태로 시작하므로 격자와 숫자가 바로 보입니다.
  `--zoom` 은 `fit`(또는 `0`), `1`(1:1), `2`, `4`, `8`, `16`, `32` \u{2026} 를 받고
  내부 줌 단계 `0.125`\~`256` 범위로 맞춰집니다. 이 값은 `.av.ini` 의 `zoom=` 으로
  영속화되므로, 다음 실행 때도 같은 배율로 시작합니다(2장 참조).
]

== 픽셀 값 표시 형식 순환 \u{2014} #key("Ctrl+X")

#key("Ctrl+X") 를 누를 때마다 *정수* 픽셀값의 표기 방식이 3단계로 순환합니다.
풍선말, 줌 오버레이, diff 값 표시가 모두 동시에 바뀝니다.

#table(
  columns: (auto, auto, 1fr),
  [*단계*], [*표기*], [*설명*],
  [`0` Decimal], [`128`], [10진수. 기본값],
  [`1` Hex 0x], [`0x80`], [C 스타일 16진수. 최소 2자리 0 패딩(`%02X`)],
  [`2` Hex h], [`80h`], [Intel\/어셈블러 스타일 16진수. 최소 2자리 0 패딩],
)

메뉴에서는 `View > Pixel Format` 아래의 `Decimal (128)` \/ `Hex (0x80)` \/
`Hex (80h)` 항목으로 직접 고를 수 있습니다(현재 선택 항목에 체크 표시).
선택은 `$HOME/.av.ini` 에 `pixel_format=0|1|2` 로 영속화됩니다.

#note[
  0 패딩은 "최소" 2자리입니다. 10\-bit 원본값 `641` 은 `0x281` 처럼 3자리로
  그대로 나옵니다. 값이 잘리지 않습니다.
]

#warn[
  HDR float 값(`%.2f` \/ `%.3f`)은 16진 순환 대상이 아닙니다. HDR 영상에서
  #key("Ctrl+X") 를 눌러도 표시가 변하지 않는 것은 정상입니다.
]

#tip[
  레지스터 덤프나 RTL 시뮬레이션 로그와 대조할 때는 `Hex 0x` 또는 `Hex h` 로
  바꿔 두면 눈으로 값을 변환할 필요가 없습니다. DDI 보상 LUT 의 엔트리를
  16진으로 관리하는 팀이라면 첫 실행에서 한 번만 바꿔 두면 이후 계속 유지됩니다.
]

== 채널 선택 \u{2014} #key("Shift+R") \/ #key("Shift+G") \/ #key("Shift+B") \/ #key("Shift+Y") \/ #key("Shift+C")

한 채널만 분리해서 보면 특정 서브픽셀에만 걸린 보상 오차나 컬러 필터 계열
아티팩트가 훨씬 잘 드러납니다.

#table(
  columns: (auto, 1fr),
  [*키*], [*동작*],
  [#key("Shift+R")], [Red 채널만 회색조로 표시],
  [#key("Shift+G")], [Green 채널만 회색조로 표시],
  [#key("Shift+B")], [Blue 채널만 회색조로 표시],
  [#key("Shift+Y")], [Rec.709 루마 Y\u{2032} 그레이뷰 토글(다시 누르면 RGB 복귀)],
  [#key("Shift+C")], [RGB 전체로 복귀(리셋)],
)

Y\u{2032} 그레이뷰는 감마 인코딩된 표시값에 Rec.709 계수
`Y' = 0.2126 R' + 0.7152 G' + 0.0722 B'` 를 적용합니다. GPU 셰이더와 소프트웨어
렌더러가 동일한 계수를 씁니다.

#warn[
  여기서의 Y\u{2032} 는 *비디오 루마*(감마 인코딩된 값에 적용)이고, #key("Shift+V")
  컬러메트리와 `--probe` 가 쓰는 CIE Y 는 *선형광 휘도*입니다. 계수 숫자가 거의
  같아 보이지만 적용 도메인이 다르므로 두 값을 같은 것으로 취급하면 안 됩니다.
  이 구분은 `src/color.h` 주석에 명시되어 있습니다.
]

=== diff 모드와의 상호작용

채널 모드는 표시 필터가 아니라 *diff 연산에도 함께 적용*됩니다.

#table(
  columns: (auto, 1fr),
  [*diff 모드*], [`Shift+R/G/B` 가 켜졌을 때의 동작],
  [Absolute \/ Relative], [해당 채널의 차이만 회색조로 표시],
  [Highlight], [해당 채널의 차이만 임계 판정에 사용],
  [FalseColor], [3채널 평균이 아니라 해당 채널의 차이를 컬러맵에 매핑],
  [Enhance], [해당 채널만 `[min,max]` \u{2192} `[128,255]` 로 재매핑],
  [AlphaBlend], [혼합 결과의 해당 채널만 회색조로 표시],
  [Signed], [항상 Rec.709 루마 기준 \u{2014} 채널 모드 영향 없음],
)

톨러런스 임계값(4장 참조)도 채널 모드를 따릅니다. RGB 모드에서는 3채널 최대
차이와 비교하지만, 단일 채널 모드에서는 그 채널의 차이만 임계와 비교합니다.
"Identical" 오버레이와 Diff Listing 창의 동일 판정 역시 채널별로 따로
평가되므로, "RGB 로는 다르지만 G 채널만 보면 완전히 같다"는 상태를 바로
확인할 수 있습니다.

#warn[
  #key("Shift+Y") 의 Y\u{2032} 그레이뷰는 A\/B 패널에만 적용됩니다. diff 셰이더는
  R\/G\/B 단일 채널만 처리하므로, 루마 모드에서 Diff 패널은 RGB diff 그대로
  그려집니다. 다만 픽셀 값 풍선말은 루마 모드에서 R\/G\/B 세 값을 모두 보여 줍니다.
]

채널 모드는 `.av.ini` 에 `channel_mode=0|1|2|3|4`(RGB\/R\/G\/B\/Luma)로
영속화됩니다.

#ex("서브픽셀 렌더링 보상의 채널 편중 확인")[
  ```
  av --diff-mode falsecolor --amplify 8 orig.png comp.png
  ```
  #key("Shift+R") \u{2192} #key("Shift+G") \u{2192} #key("Shift+B") 로 채널을 돌려
  보면서 FalseColor 지도의 밝기 분포를 비교합니다. R 에서만 광범위하게 색이
  올라온다면 보상 IP 의 R 채널 계수 또는 R 서브픽셀 경로를 의심합니다.
  #key("Shift+C") 로 언제든 RGB 전체 보기로 돌아옵니다.
]

== 컬러메트리 풍선말 \u{2014} #key("Shift+V")

#key("Shift+V") 는 픽셀 풍선말 아래에 CIE 색도 정보 상자를 덧붙입니다. 켜는
순간 #key("V") 픽셀 풍선말도 자동으로 함께 켜집니다(색도 상자는 풍선말에 얹혀
그려지기 때문). 메뉴로는 `View > Colorimetry Probe (xy/CCT)` 입니다.

색도 상자는 최대 3줄입니다.

#table(
  columns: (auto, 1fr),
  [*줄*], [*형식과 내용*],
  [A], [`A xy 0.3096,0.3249  6714K` \u{2014} A 영상의 CIE 1931 `x,y`(소수 4자리)와 CCT(K, 정수)],
  [B], [`B xy 0.3064,0.3218  6930K` \u{2014} B 영상의 같은 값(B 가 로드된 경우만)],
  [Δ], [`Δu'v' 0.0024  ΔCCT 216K` \u{2014} A\/B 모두 유효할 때만. u\u{2032}v\u{2032}
       평면상의 유클리드 거리와 CCT 차(B \u{2212} A)],
)

A\/B 두 줄은 *커서가 어느 패널 위에 있든* 항상 `images[0]`(A)과 `images[1]`(B)을
같은 이미지 좌표에서 샘플링합니다. 즉 커서 한 번으로 두 영상의 색도를 동시에
읽습니다.

#table(
  columns: (auto, 1fr),
  [*입력 종류*], [*XYZ 변환 방식*],
  [PNM 원본값], [`값 / maxval` 을 sRGB 표시값으로 보고 감마 해제 후 매트릭스 적용],
  [HDR float], [이미 선형광으로 보고 매트릭스만 적용],
  [LDR uint8], [`값 / 255` 를 sRGB 표시값으로 보고 감마 해제 후 매트릭스 적용],
)

기준 백색점은 CIE 2도 표준광 D65(`x=0.31270, y=0.32900`)이며, sRGB\u{2192}XYZ
Bradford 적응 매트릭스가 순백 `(1,1,1)` 을 정확히 이 점으로 보냅니다. CCT 는
McCamy 1992 3차 근사식을 씁니다.

=== 헤드리스 `--probe` 와 같은 색 코어

GUI 풍선말과 헤드리스 `--probe` 는 `src/color.cpp` 라는 *하나의 색 코어*를
공유합니다(전부 double 정밀도, GL\/SDL 의존 없음). 따라서 화면에서 읽은 값과
CI 에서 뽑은 CSV 값이 반올림 자릿수만 다를 뿐 동일합니다. FLIP 엔진이 내부적으로
쓰는 자체 D65 와는 의도적으로 분리되어 있어, 색도 수치는 공개 레퍼런스와
맞춰집니다.

GUI 는 화면 공간을 아끼기 위해 `x,y` \/ CCT \/ Δu\u{2032}v\u{2032} \/ ΔCCT 만
보여 줍니다. XYZ, u\u{2032}v\u{2032} 원값, Duv, L\*, ΔE76 까지 필요하면 헤드리스
`--probe` 를 쓰십시오(8장 참조).

#ex("한 픽셀의 색도 이동량 정량화")[
  화면에서 좌표를 찍고(예: 풍선말이 `(4, 4)` 라고 표시), 같은 좌표를 CLI 로 넘깁니다.
  ```
  av --probe 4,4 orig.ppm comp.ppm
  ```
  ```
  which,X,Y,Z,x,y,uprime,vprime,CCT,Duv,Lstar
  A,0.799808,0.839443,0.944302,0.309577,0.324918,0.197187,0.465657,6713.57,0.00270091,93.4263
  B,0.797736,0.837855,0.967665,0.306438,0.321849,0.196142,0.463513,6929.78,0.00275288,93.3572
  delta,,,,,,,,216.203,0.00238477,1.6842
  ```
  `delta` 행은 XYZ\/xy\/u\u{2032}v\u{2032} 열을 비우고 CCT 열에 ΔCCT, Duv 열에
  Δu\u{2032}v\u{2032}, Lstar 열에 ΔE76 을 담습니다. 이 예에서 보상 결과는
  CCT 가 216 K 올라갔고 Δu\u{2032}v\u{2032} 는 0.0024 \u{2014} 일반적인 화이트포인트
  관리 기준(0.004 이내)에는 들어가지만 ΔE76 1.68 은 육안 식별 한계(약 1.0) 를
  넘어섭니다.
]

#note[
  `show_colorimetry` 는 `.av.ini` 에 저장되지 않습니다. 창을 닫으면 꺼진 상태로
  돌아갑니다(반면 `show_pixel_info` 는 저장됩니다).
]

== 결함 픽셀 오버레이 \u{2014} #key("/")

#key("/") 는 float\/HDR 영상에서 물리적으로 있을 수 없는 값을 색으로 칠해 주는
오버레이를 토글합니다. 렌더러\/보상 IP 시뮬레이터가 뱉은 float 출력에 NaN 이
섞였는지, 클리핑이 빠져 음수나 초과백색이 남았는지를 한눈에 잡아냅니다.

#table(
  columns: (auto, auto, 1fr),
  [*분류*], [*색*], [*조건 (R\/G\/B 중 하나라도)*],
  [NaN], [마젠타], [`isnan(v)` \u{2014} 최우선. 즉시 확정],
  [Inf], [빨강], [`isinf(v)` \u{2014} 두 번째 우선],
  [negative], [파랑], [`v < 0` \u{2014} 초과백색보다 우선],
  [superwhite], [주황], [`v > 1`],
)

패널 좌상단에 `bad-px: NaN magenta / Inf red / neg blue / >1 orange` 범례가
같이 뜹니다. 마커 한 변의 크기는 `max(zoom, 3)` px 이라 축소 상태에서도 3 px
이상으로 보이며, 한 프레임\/패널당 최대 30,000 개까지만 그립니다(더 많으면
나머지는 생략되므로 정확한 개수는 `--validate` 로 세십시오).

#warn[
  대상은 `pixels_f32` 를 가진 영상뿐입니다. 8\-bit LDR 영상은 구조적으로 결함
  값이 나올 수 없으므로 아무것도 표시되지 않습니다. `maxval > 255` 인 PNM 도
  float 버퍼가 `0`\~`1` 로 정규화되어 있어 검출 대상이 없습니다. 이 토글은
  `.av.ini` 에 저장되지 않습니다.
]

=== `--validate` 와의 관계

GUI 오버레이가 "어디에" 있는지를 보여 준다면, 헤드리스 `--validate` 는 "몇 개"
인지를 세고 CI 게이트로 쓸 수 있는 종료 코드를 돌려줍니다. 두 경로는 완전히
같은 4분류(`nan` \/ `inf` \/ `negative` \/ `superwhite`)를 씁니다.

```
av --validate frame_hdr.hdr
```
```
class,count
nan,0
inf,0
negative,0
superwhite,0
```

0 이 아닌 항목이 있으면 stdout 끝에 최초 위반 픽셀 좌표가 최대 20개까지
`# class,x,y,channel` 형태의 주석 줄로 덧붙고, 요약 한 줄은 stderr 로 나갑니다.
종료 코드는 NaN 또는 Inf 가 하나라도 있으면 `8`, 그 외에는 `0` 입니다.
음수\/초과백색만 있는 경우는 실패로 보지 않습니다.

#tip[
  CI 에서는 `av --validate` 를 게이트로 걸어 NaN\/Inf 유입을 막고, 실패한 프레임만
  GUI 로 열어 #key("/") 로 위치를 확인하는 2단 구성이 효율적입니다.
  8\-bit 영상에 `--validate` 를 걸면 "nothing to validate" 를 stderr 로 알리고
  모든 카운트를 0 으로 출력한 뒤 `0` 으로 끝납니다.
]

== 이미지 정보 창 \u{2014} #key("I")

#key("I") 는 폭 600 px 의 반투명(85 %) `Image Info` 창을 토글합니다. 메뉴로는
`View > Show Image Info` 이며, 상태는 `.av.ini` 의 `show_info` 로 영속화됩니다.
A 또는 B 중 하나라도 로드되어 있어야 창이 뜹니다.

창에 실제로 표시되는 항목은 다음이 전부입니다.

#table(
  columns: (auto, 1fr),
  [*필드*], [*내용*],
  [`A(Left):` \/ `B(Right):`], [파일 경로 전체(경로가 비면 `(loaded)`). A 는 마젠타,
    B 는 노랑 라벨],
  [`W x H  ch=N`], [픽셀 해상도와 채널 수. `[HDR]` 태그는 Radiance 계열일 때만 붙음],
  [`C(Right):`], [`--comp` 모드에서만. img3 의 경로와 크기 (시안 라벨)],
  [`A Zoom: ... Pan: (...)`], [패널별 줌 배율(%)과 팬 오프셋. fit 상태면 `Zoom: Fit-to-Window`],
  [`PSNR: xx.xx dB`], [A(Left)=기준, B(Right)=비교. 창이 열리면 자동 계산],
)

PSNR 값은 R\/G\/B 채널별 PSNR 의 산술 평균이며, 색으로 등급이 보입니다:
40 dB 초과는 초록, 30\~40 dB 는 노랑, 30 dB 이하는 빨강입니다. 두 영상이 완전히
같으면 `PSNR: inf  (identical)` (초록), 크기나 버퍼 종류가 맞지 않으면
`PSNR: N/A (size/format mismatch)` (빨강)가 나옵니다. 영상을 다시 로드하거나
시퀀스를 넘기면 자동으로 재계산됩니다.

`--comp` 모드에서는 PSNR 이 두 줄로 바뀝니다.

```
PSNR img2(mid)   vs orig: 42.13 dB
PSNR img3(right) vs orig: 38.90 dB
```

#warn[
  `ch=` 는 항상 `4` 입니다. 로더가 모든 입력을 RGBA 4채널로 강제 변환하기
  때문이며, 원본 파일의 채널 수(그레이스케일 1채널 등)를 뜻하지 않습니다.
  또한 이 창에는 *비트 깊이 필드가 없습니다.* 원본 비트 깊이는 #key("V") 풍선말의
  값 범위로 판단하십시오 \u{2014} 값이 `255` 를 넘어가면 10\-bit 이상 PNM 원본값이
  살아 있다는 뜻입니다. 픽셀 통계\/히스토그램은 6장을 참조하십시오.
]

== 지원 포맷 상세

av 는 PNM 계열만 자체 파서로 읽고, 나머지는 stb\_image 로 디코딩합니다.
파일 열기 대화상자의 필터는 `png;jpg;jpeg;bmp;tga;hdr;ppm;pgm` 입니다.

#table(
  columns: (auto, 1fr),
  [*포맷*], [*지원 내용과 주의점*],
  [PNG], [8\/16\-bit, 알파 포함. *16\-bit PNG 는 디코딩 시 8\-bit 로 눌립니다* \u{2014}
    원본 비트 깊이를 보존하려면 16\-bit PNM 을 쓰십시오],
  [JPEG], [표준 손실 압축. 블록 아티팩트가 diff 에 그대로 잡히므로 보상 검증
    입력으로는 부적합],
  [BMP], [Windows 비트맵. 무손실이지만 8\-bit 고정],
  [TGA], [Targa. 알파 채널 지원, 8\-bit 고정],
  [HDR], [Radiance RGBE. float32 RGBA 로 로드되어 `pixels_f32` 를 채움 \u{2014}
    #key("/") 결함 오버레이와 `--validate` 의 유일한 실질 대상],
  [PNM P5\/P6], [PGM\/PPM Binary. 자체 파서. maxval `1`\~`65535`, 16\-bit 원본값
    보존. `maxval > 255` 이면 정밀 diff 용 float 버퍼도 함께 생성],
  [PNM P2\/P3], [PGM\/PPM ASCII. 자체 파서. 원본값 보존.
    `#` 주석 줄 허용. 파일이 크면 로딩이 느립니다],
)

P5\/P2(그레이스케일)는 한 값을 R\=G\=B 로 복제해 3채널로 확장한 뒤
`pixels_orig` 에 담습니다. 따라서 그레이스케일 PNM 에서도 풍선말은 세 값을
동일하게 보여 줍니다.

#warn[
  *헤드리스 경로는 PNM ASCII(P2\/P3)를 읽지 못합니다.* `--metrics`,
  `--probe`, `--validate`, `--uniformity`, `--diff-out`, `--comp-out` 등
  헤드리스 모드는 stb\_image 기반의 CPU 디코더만 쓰므로 P5\/P6 바이너리는
  되지만 P2\/P3 는 `failed to load` 로 종료 코드 `4` 를 냅니다. 같은 파일을 GUI 로
  열면 잘 보이므로 혼동하기 쉽습니다. CI 파이프라인에서는 PNM 을 *바이너리
  P5\/P6* 로 덤프하십시오. 덧붙여 헤드리스 디코더는 16\-bit PNM 도 8\-bit 로
  변환해 읽으므로, 원본 비트 깊이가 필요한 정밀 비교는 GUI 에서 수행해야 합니다.
]

#warn[
  치트시트에는 JPEG 의 EXIF 방향이 자동 적용된다고 적혀 있으나, 실제 디코더
  (stb\_image)는 EXIF 방향 태그를 해석하지 않습니다. 스마트폰으로 찍은 세로
  사진이 눕는다면 #key("R") 시계 방향 90도 \/ #key("Ctrl+R") 반시계 방향 90도
  로 직접 회전시키십시오. 회전은 A\/B 양쪽에 동시에 적용되고 뷰는 fit 으로
  초기화됩니다.
]

#tip[
  DDI 보상 검증용 참조\/비교 쌍은 다음 조합을 권장합니다.

  - 8\-bit 파이프라인: PNG(무손실, 알파 불필요 시 3채널)
  - 10\/12\/16\-bit 파이프라인: P6 바이너리 PNM(maxval 을 실제 비트폭에 맞춤)
  - 선형 HDR 파이프라인: Radiance `.hdr`

  세 경우 모두 A\/B 의 해상도와 버퍼 종류가 같아야 PSNR\/SSIM\/FLIP 이 계산됩니다.
  파일 저장\/시퀀스 취급은 7장, 색 관리와 LUT 적용은 9장을 참조하십시오.
]
