#import "_common.typ": *

= 분석 도구 창

3장에서 배운 픽셀 단위 검사와 4장의 Diff 모드가 "한 점" 또는 "한 화면"을
보는 도구였다면, 이 장의 분석 창들은 이미지 전체 또는 지정한 영역을
숫자와 그래프로 환산해 줍니다. 히스토그램, 수평·수직 절단면, 전체 통계,
ROI 통계, 산점도의 다섯 가지 창과 이들을 PNG·CSV로 내보내는 방법을 다룹니다.
DDI 보상 IP 출력을 원본과 대조할 때, "눈으로는 비슷한데 수치로는 어떤가"를
답하는 곳이 바로 이 장입니다.

== 다섯 개의 창과 공통 규칙

=== 창 목록

#table(
  columns: (auto, auto, 1fr),
  [*키*], [*창 제목*], [*대상*],
  [#key("Ctrl+H")], [`Histogram`],            [A / B / `Diff |A-B|`],
  [#key("Ctrl+L")], [`H-Line Cut`],           [A / B / `Diff |A-B|`],
  [#key("Ctrl+Y")], [`V-Line Cut`],           [A / B / `Diff |A-B|`],
  [#key("Ctrl+S")], [`Image Statistics`],     [A / B / `Diff |A-B|`],
  [#key("Ctrl+E")], [`ROI Statistics`],       [ROI 안의 A / B / `Diff |A-B|`],
  [#key("Ctrl+T")], [`Scatter Plot  (A vs B)`], [A vs B 픽셀쌍],
)

다섯 창 모두 상단 메뉴의 `View` 에서도 같은 항목으로 열 수 있으며,
메뉴에는 위 단축키가 그대로 표시됩니다.

#fig_analysis_windows()

=== 상호 배타 규칙

#key("Ctrl+H") / #key("Ctrl+L") / #key("Ctrl+Y") / #key("Ctrl+S") 네 창은
서로 배타적입니다. 하나를 켜면 나머지 셋이 자동으로 닫힙니다.
화면 대부분을 차지하는 큰 창들이라 겹쳐 놓아도 읽을 수 없기 때문입니다.

반면 `ROI Statistics`(#key("Ctrl+E"))와 `Scatter Plot`(#key("Ctrl+T"))은
독립적이어서 위 네 창 중 하나와 동시에 띄워 둘 수 있습니다.

=== 이동 · 크기 · 닫기

#table(
  columns: (auto, 1fr),
  [*조작*], [*방법*],
  [이동],   [창 제목 표시줄을 좌드래그],
  [크기],   [창 가장자리 또는 오른쪽 아래 모서리를 좌드래그],
  [닫기],   [제목 표시줄 오른쪽 `x` 버튼, 같은 단축키 다시 누르기, 또는 #key("Esc")],
)

처음 열릴 때의 기본 크기는 다음과 같습니다.

#table(
  columns: (auto, 1fr),
  [*창*], [*첫 표시 크기*],
  [`Histogram` / `H-Line Cut` / `V-Line Cut` / `Image Statistics`], [작업 영역의 가로 90% \u{00D7} 세로 85%],
  [`Scatter Plot`], [작업 영역의 가로 50% \u{00D7} 세로 70%],
  [`ROI Statistics`], [가로 600 px, 세로는 내용에 맞춰 자동],
)

#note[
  창을 옮기거나 크기를 바꾸면 그 위치·크기는 프로그램을 실행한 디렉터리의
  `.av_imgui.ini` 에 자동 저장되어 다음 실행 때 복원됩니다.
  한편 "어떤 창이 열려 있었는가"는 홈 디렉터리의 `~/.av.ini` 에
  `show_histogram` / `show_hline_cut` / `show_vline_cut` / `show_stats` /
  `show_scatter_plot` / `histogram_compare` 키로 저장됩니다.
  ROI 통계 창의 열림 상태(`show_roi_stats`)만은 저장되지 않아,
  다시 실행하면 항상 닫힌 상태로 시작합니다.
]

#warn[
  #key("Esc") 는 "열려 있는 것 중 하나"를 정해진 우선순위로 닫습니다.
  분석 창끼리의 순서는 `Histogram` \u{2192} `H-Line Cut` \u{2192} `V-Line Cut`
  \u{2192} `Image Statistics` \u{2192} `ROI Statistics` \u{2192} `Scatter Plot`
  이고, 그 뒤로 Image Info · 픽셀 풍선말 · 저장 창 · Crosshair · ROI 모드가
  이어집니다. 여러 개를 띄워 놓았다면 #key("Esc") 를 여러 번 눌러야 모두
  닫힙니다.
]

=== 어느 이미지가 A 이고 어느 것이 B 인가

분석 창의 A / B 는 언제나 *로드 슬롯*을 가리킵니다.

#table(
  columns: (auto, 1fr),
  [*라벨*], [*가리키는 것*],
  [A], [명령줄의 첫 번째 이미지 인자 (또는 `Open` 대화상자로 왼쪽 패널에 연 이미지)],
  [B], [명령줄의 두 번째 이미지 인자 (오른쪽 패널)],
)

```
av orig.png comp_ip_out.png
# → A = orig.png, B = comp_ip_out.png
```

다음 세 가지 규칙을 기억하면 헷갈릴 일이 없습니다.

+ *#key("Shift+Space") 의 A/B 화면 스왑은 분석 창에 영향을 주지 않습니다.*
  화면상 좌우가 바뀌어도 분석 창의 A 는 여전히 첫 번째 인자입니다.
+ *`--comp` 모드에서 #key("S") 로 img2 \u{2194} img3 을 교환하면 B 통계가 바뀝니다.*
  이 키는 두 번째 슬롯의 내용 자체를 바꾸므로 분석 창도 새 이미지를 읽습니다
  (5장 참조).
+ *`Diff |A-B|` 섹션은 항상 A 기준입니다.* 절단면 창의 Diff 섹션은
  A 패널의 pan 값으로 행·열을 정합니다.

#note[
  `Histogram` / `H-Line Cut` / `V-Line Cut` / `Image Statistics` 창의
  `Diff |A-B|` 섹션은 *두 이미지가 모두 로드되어 있고 Diff 모드가 켜져 있을 때만*
  나타납니다 (#key("D") 등, 4장 참조). Diff 모드가 `none` 이면 A 섹션과 B 섹션만
  보입니다. 예외적으로 `ROI Statistics` 의 `ROI Diff |A-B|` 표는 Diff 모드와
  무관하게 두 이미지만 로드되어 있으면 언제나 계산됩니다.
]

== 히스토그램 \u{2014} #key("Ctrl+H")

=== 무엇이 보이는가

#key("Ctrl+H") 를 누르면 `Histogram` 창이 열리고, 로드된 이미지마다 한 섹션씩
쌓입니다. 각 섹션은 파일 이름을 제목으로 하고 그 아래 R, G, B *세 개의 차트로
분리*되어 그려집니다. 세 채널을 한 그래프에 겹쳐 그리지 않기 때문에 채널별
클리핑이나 계조 결손을 서로 가리지 않고 볼 수 있습니다.

#table(
  columns: (auto, 1fr),
  [*요소*], [*의미*],
  [섹션 제목], [`A: orig.png` / `B: comp_ip_out.png` / `Diff |A-B|`],
  [빈(bin) 수], [항상 256 개 (X축 `0`\~`255`)],
  [X축 눈금], [`0, 32, 64, 96, 128, 160, 192, 224, 255` 고정],
  [Y축 눈금], [실제 픽셀 개수. 천 단위 콤마 표기 (예: `1,234,567`)],
  [막대 색], [R = 빨강, G = 초록, B = 파랑],
)

#warn[
  8비트 이미지는 픽셀값이 그대로 빈 번호가 되지만, float/HDR 이미지는
  그 이미지 안의 최댓값으로 나눈 뒤 `0`\~`255` 로 환산해 담습니다.
  즉 float 이미지의 X축은 "절대 코드값"이 아니라 "그 이미지 최댓값 대비 비율"
  입니다. 절대값이 필요하면 통계 창(#key("Ctrl+S"))의 `Min` / `Max` 행을 함께 보세요.
]

=== 로그 스케일

세로축은 `log1p(count)` 로 정규화됩니다. 자연 이미지의 히스토그램은 특정 계조에
수십만 개가 몰리고 나머지는 수십 개뿐이라, 선형 축으로는 꼬리가 완전히 보이지
않기 때문입니다. 정규화는 *채널마다 독립적으로* 그 채널의 최댓값을 기준으로
수행되므로, R 차트의 꼭대기와 G 차트의 꼭대기는 서로 다른 개수를 뜻할 수
있습니다. 실제 개수는 Y축 눈금 라벨에 그대로 적혀 있으니 그 숫자를 읽으면 됩니다.

#tip[
  DDI 보상 결과에서 "계조 결손(빠진 코드)"을 찾을 때 로그 스케일이 결정적입니다.
  선형 축이라면 개수 0 인 빈과 개수 5 인 빈이 똑같이 바닥에 붙어 보이지만,
  로그 축에서는 개수 0 인 빈만 완전히 비어 있는 세로 틈으로 드러납니다.
]

=== 값 읽기 \u{2014} 호버 크로스헤어

차트 위에 마우스를 올리면 세 채널 차트를 세로로 관통하는 흰 크로스헤어가
그려지고, 풍선말에 그 빈의 정확한 값이 표시됩니다.

```
Bin: 128
─────────
R: 12,048
G: 11,730
B: 9,415
```

한 번의 호버로 R/G/B 세 값을 동시에 읽을 수 있으므로, 특정 코드값에서
채널 간 분포 편차를 비교하기에 좋습니다.

=== `Diff |A-B|` 히스토그램

두 이미지가 로드되어 있고 Diff 모드가 켜져 있으면 세 번째 섹션으로
`Diff |A-B|` 히스토그램이 추가됩니다. 8비트일 때 X축은 채널별 절대 오차
`0`\~`255` 를 그대로 나타냅니다. 즉 *빈 0의 막대 높이 = 완전히 일치한 픽셀 수*
이고, 오른쪽으로 갈수록 오차가 큰 픽셀입니다.

#ex("보상 오차 분포 확인")[
  ```
  av orig.png comp_ip_out.png --diff-mode abs
  ```
  #key("Ctrl+H") 로 히스토그램을 연 뒤 `Diff |A-B|` 섹션을 봅니다.
  막대가 빈 0\~2 에만 몰려 있으면 LSB 수준의 반올림 차이뿐이고,
  빈 8 근처에 두 번째 봉우리가 생겼다면 특정 계조 구간에서 보상 LUT가
  한 단계 어긋났다는 신호입니다. 그 봉우리에 마우스를 올려 정확한
  픽셀 개수를 읽고, 4장의 `highlight` 모드로 그 픽셀들이 화면 어디에
  모여 있는지 확인하십시오.
]

=== A/B 동시 표시 \u{2014} `Compare A/B`

두 이미지가 모두 로드되면 창 맨 아래에 `Compare A/B` 체크박스가 나타납니다.
체크하면 `Compare A (magenta) / B (yellow)` 라는 제목의 추가 차트 3개(R/G/B)가
그려지고, 한 차트 위에 A는 마젠타, B는 노랑 반투명 막대로 *겹쳐서* 그려집니다.

#table(
  columns: (auto, 1fr),
  [*항목*], [*동작*],
  [A 막대],  [마젠타 (반투명)],
  [B 막대],  [노랑 (반투명)],
  [정규화],  [A와 B의 공통 최댓값을 기준으로 로그 정규화 \u{2014} 두 곡선의 높이를 직접 비교 가능],
  [입력],    [8비트 픽셀 버퍼 기준],
  [영속화],  [`~/.av.ini` 의 `histogram_compare` 로 저장 (기본 꺼짐)],
)

#tip[
  *언제 여는가:* 보상 IP가 전체 밝기나 감마를 통째로 밀어 버렸는지 한눈에
  판정할 때. `Compare A/B` 를 켰을 때 노랑 분포가 마젠타 분포에 대해 통째로
  오른쪽으로 평행 이동했다면 오프셋 편차, 오른쪽으로 늘어났다면 게인(감마)
  편차입니다. 모양은 같은데 특정 구간만 빗살처럼 비었다면 LUT 양자화 문제입니다.
]

== 수평 절단면 \u{2014} #key("Ctrl+L") / 수직 절단면 \u{2014} #key("Ctrl+Y")

=== 무엇이 보이는가

#key("Ctrl+L") 은 `H-Line Cut`(가로 한 줄), #key("Ctrl+Y") 는
`V-Line Cut`(세로 한 줄) 창을 엽니다. 두 창의 구조는 완전히 같고,
가로/세로만 다릅니다. 히스토그램과 마찬가지로 이미지마다 한 섹션,
섹션마다 R/G/B 세 개의 꺾은선 차트가 그려집니다.

#table(
  columns: (auto, auto, 1fr),
  [*창*], [*샘플하는 선*], [*가로축*],
  [`H-Line Cut`], [한 개의 행 (row)], [열 좌표 `0`\~`width-1`],
  [`V-Line Cut`], [한 개의 열 (column)], [행 좌표 `0`\~`height-1`],
)

섹션 제목에는 파일 이름과 함께 지금 잘라 낸 위치가 표시됩니다.

```
A: orig.png  row=540
B: comp_ip_out.png  row=540
Diff |A-B|  row=540
```

=== 어느 행 · 어느 열이 잘리는가 (중요)

절단 위치는 마우스 커서가 아니라 *그 패널 뷰포트의 정중앙*입니다.
내부적으로 다음과 같이 계산됩니다.

```
row = round(-pan_y + height / 2)      # H-Line Cut
col = round(-pan_x + width  / 2)      # V-Line Cut
```

따라서 *보고 싶은 줄을 패널 한가운데로 pan 해서 가져오는 것*이 사용법입니다.
#key("j") / #key("k") 로 위아래, #key("h") / #key("l") 로 좌우 1픽셀씩 이동하고,
#key("Shift") 를 함께 누르면 `--pan-step` 만큼(기본 32픽셀) 점프합니다
(2장 참조). 절단면 창을 띄워 둔 채 pan 하면 곡선이 실시간으로 갱신됩니다.

#tip[
  #key("S") 로 뷰포트 동기화(`sync_viewports`, 기본 켜짐)를 유지하면 A 패널과
  B 패널의 pan 이 같아져 A · B · Diff 세 섹션이 *정확히 같은 행/열*을 봅니다.
  동기화를 꺼 두면 A는 540행, B는 300행을 보는 상태가 될 수 있으니 주의하십시오.
  Diff 섹션은 어떤 경우든 A 패널의 pan 을 기준으로 합니다.
]

=== 축과 정규화

#table(
  columns: (auto, 1fr),
  [*요소*], [*의미*],
  [X축 눈금], [`1` / `2` / `5` \u{00D7} 10의 거듭제곱 규칙으로 고른 "예쁜" 픽셀 좌표 눈금 (최대 7구간)],
  [Y축 눈금 (8비트)], [실제 코드값. 최댓값 255 기준 4등분 (`64 / 128 / 191 / 255`)],
  [Y축 눈금 (float)], [그 선 안의 최댓값 기준 4등분, 소수점 2자리로 표시],
  [곡선 점 수], [차트 폭(px)과 데이터 길이 중 작은 쪽으로 균등 다운샘플],
)

즉 4K 이미지의 3840픽셀짜리 행을 900픽셀 폭 차트에 그릴 때는 900점으로
균등 추출해 그립니다. 세밀한 스파이크를 놓치지 않으려면 창을 넓히거나
최대화하십시오.

=== 값 읽기 \u{2014} 호버 크로스헤어

차트 위에 마우스를 올리면 세 채널을 관통하는 크로스헤어와 함께 풍선말이 뜹니다.

```
X: 1920            # V-Line Cut 에서는 "Y: 540"
─────────
R: 128
G: 130
B: 127
```

8비트 이미지는 정수 코드값으로, float 이미지는 소수점 4자리로 표시됩니다.

=== `Diff |A-B|` 절단면

Diff 모드가 켜져 있으면 세 번째 섹션에 채널별 절대 오차 곡선이 그려집니다.
평평하게 바닥에 붙어 있어야 정상이고, 특정 구간에서 솟아오르면 그 좌표 구간의
보상이 틀어진 것입니다.

#ex("가로 밴딩 / 무라 라인 프로파일 확인")[
  ```
  av flat_gray128.png comp_flat_gray128.png --diff-mode abs
  ```
  플랫 필드 패턴에서 세로 방향 밴딩이 의심되면 #key("Ctrl+Y") 로
  `V-Line Cut` 을 엽니다. 정상이라면 세 채널 모두 128 근처의 평탄한 직선이어야
  합니다. B 섹션 곡선에 주기 8픽셀의 톱니가 보이면 게이트 드라이버 블록 경계의
  무라이고, 완만한 사인파 모양이면 전원 리플이나 감마 탭 보간 오차입니다.
  #key("Shift+j") 로 몇 줄씩 옮겨 가며 그 주기가 열 방향으로 유지되는지
  확인하면 "라인 결함"인지 "면 결함"인지 가려낼 수 있습니다.
  마찬가지로 가로 줄무늬는 #key("Ctrl+L") 로 확인합니다.
]

== 통계 창 \u{2014} #key("Ctrl+S")

=== 무엇이 보이는가

#key("Ctrl+S") 는 `Image Statistics` 창을 엽니다. 이미지마다 한 개의 표가
쌓이며, 표 제목에는 파일 이름과 해상도가 붙습니다
(예: `A: orig.png  (3840 x 2160)`). A 섹션 제목은 마젠타, B는 노랑,
`Diff |A-B|` 는 시안색으로 구분됩니다.

표는 5열 구조입니다: `Metric` \u{007C} `Red (R)` \u{007C} `Green (G)` \u{007C}
`Blue (B)` \u{007C} `Description`.

=== 표시되는 통계량 전수

#table(
  columns: (auto, 1fr),
  [*Metric*], [*의미*],
  [`Pixels`],            [이미지의 전체 픽셀 수 (width \u{00D7} height). 천 단위 콤마 표기],
  [`Min`],               [채널 최솟값],
  [`Max`],               [채널 최댓값],
  [`Dynamic Range`],     [`Max - Min`],
  [`Mean`],              [산술 평균],
  [`Std Dev`],           [표준편차 \u{2014} 평균 주위의 퍼짐],
  [`Variance`],          [분산 (표준편차의 제곱, 모분산)],
  [`Median`],            [중앙값 \u{2014} 이상치에 평균보다 강건],
  [`Skewness`],          [왜도. 0 = 대칭, 양수 = 오른쪽 꼬리, 음수 = 왼쪽 꼬리],
  [`Kurtosis (excess)`], [초과 첨도. 0 = 정규분포, 양수 = 두꺼운 꼬리],
  [`Energy`],            [픽셀값 제곱의 평균 (원점 기준 2차 모멘트)],
  [`RMS`],               [`Energy` 의 제곱근 \u{2014} 신호 크기],
  [`Entropy (bits)`],    [Shannon 엔트로피 (bit). 높을수록 복잡·무작위],
)

`Diff |A-B|` 섹션에는 위 13개 행에 더해 다음 4개 행이 추가됩니다.

#table(
  columns: (auto, 1fr),
  [*Metric*], [*의미*],
  [`MSE`],       [평균 제곱 오차. 작을수록 유사],
  [`PSNR`],      [피크 신호 대 잡음비 (dB). 클수록 우수],
  [`MAE`],       [평균 절대 오차],
  [`Max Error`], [단일 픽셀 최대 절대 오차],
)

#note[
  `PSNR` 의 피크값은 데이터 형식을 따릅니다. 8비트 이미지는 peak = 255,
  float/HDR 이미지는 peak = 1.0 로 계산합니다. 두 이미지가 완전히 동일하면
  `inf` 로 표시됩니다. 크기가 다른 두 이미지는 픽셀 수가 작은 쪽까지만
  비교합니다. 여기의 채널별 PSNR 은 헤드리스 `--metrics` 가 출력하는 값과
  같은 정의이며, CI 게이트와 수치를 맞춰 보고 싶다면 8장을 참조하십시오.
]

#tip[
  *언제 여는가:* 보상 전후의 "전역 성질"을 한 장으로 남길 때.
  `Mean` 차이는 밝기 바이어스, `Std Dev` 차이는 대비 변화,
  `Entropy` 감소는 계조 뭉개짐(밴딩)을 뜻합니다.
  특히 보상 IP가 계조를 압축했는지 판단하려면 A와 B의
  `Dynamic Range` 와 `Entropy (bits)` 를 나란히 읽는 것이 가장 빠릅니다.
]

== ROI 선택 모드와 ROI 통계 \u{2014} #key("Ctrl+E")

=== ROI 지정하기

#key("Ctrl+E") 를 누르면 ROI(Region of Interest) 선택 모드가 켜집니다.
이 상태에서 아무 패널 위나 *좌클릭 드래그* 하면 하늘색 사각형이 따라 그려지고,
버튼을 놓는 순간 다음이 일어납니다.

+ 화면 좌표가 현재 zoom / pan 을 역산해 이미지 픽셀 좌표로 변환됩니다.
+ 사각형이 이미지 경계 안으로 잘립니다.
+ `ROI Statistics` 창이 *자동으로 열립니다*.
+ 선택 영역에 하늘색 테두리와 `1024 x 768` 형태의 크기 라벨이 남습니다.

#table(
  columns: (auto, 1fr),
  [*동작*], [*설명*],
  [드래그 크기 하한], [가로·세로 어느 쪽이든 3 px 미만이면 무시 (오클릭 방지)],
  [대상 패널],        [드래그를 시작한 패널. Diff 패널이나 Overlay 화면 위에서도 가능],
  [휠 줌],            [ROI 모드에서도 마우스 휠 줌은 그대로 동작],
  [재선택],           [다시 드래그하면 이전 ROI를 대체],
)

#note[
  ROI 는 *이미지 픽셀 좌표*로 저장되므로, 어느 패널에서 그렸든 A와 B의
  같은 좌표 영역에 동일하게 적용됩니다. 그래서 왼쪽 패널에서 결함 부위를
  지정하기만 하면 오른쪽 보상 결과의 같은 부위 통계가 자동으로 따라옵니다.
]

=== `ROI Statistics` 창의 구성

창 제목 자체가 현재 ROI를 알려 줍니다: `ROI Statistics  [1200,860  256x192]`.
본문은 위에서부터 다음 순서입니다.

+ ROI 요약 한 줄 \u{2014} `ROI: (1200, 860)  size 256 x 192  = 49,152 px`
+ `Image A (ROI)` 표 \u{2014} 통계 창(#key("Ctrl+S"))과 동일한 13개 지표를
  ROI 안의 픽셀만으로 다시 계산 (여기서 `Pixels` 행은 ROI 픽셀 수)
+ A의 ROI 평균 컬러메트리 한 줄
+ `Image B (ROI)` 표 + B의 ROI 평균 컬러메트리 한 줄
+ `ROI mean Δ (A→B)` 한 줄 \u{2014} A와 B의 색 차이
+ `ROI Diff |A-B|` 표 \u{2014} 13개 지표 + `MSE` / `PSNR` / `MAE` / `Max Error`

=== 컬러메트리 행 (`xy` / `u'v'` / `CCT`)

각 이미지 표 아래에 붙는 컬러메트리 줄은 *ROI 평균색*을 CIE 좌표로 환산한
값입니다. 8비트 이미지는 평균 sRGB 값을, float 이미지는 선형 RGB 평균을
D65 기준으로 XYZ 변환합니다.

```
   mean colorimetry: xy 0.3128,0.3290  u'v' 0.1978,0.4683  CCT 6503K  Duv +0.0001
```

#table(
  columns: (auto, 1fr),
  [*항목*], [*의미*],
  [`xy`],   [CIE 1931 색도 좌표 (소수점 4자리)],
  [`u'v'`], [CIE 1976 UCS 색도 좌표 \u{2014} 지각 균등성이 좋아 색차 판정에 사용],
  [`CCT`],  [McCamy 근사 상관 색온도 (K)],
  [`Duv`],  [흑체 궤적으로부터의 부호 있는 거리. 양수 = 녹색 쪽, 음수 = 자홍 쪽],
)

A와 B가 모두 로드되어 있으면 그 아래에 Δ 줄이 하나 더 표시됩니다.

```
ROI mean Δ (A→B):  Δu'v' 0.0021   ΔCCT -87K   ΔE76 1.34
```

#table(
  columns: (auto, 1fr),
  [*항목*], [*의미*],
  [`Δu'v'`], [A와 B 평균색의 `u'v'` 평면 유클리드 거리. 보통 0.002 이하를 "육안 무차" 기준으로 씁니다],
  [`ΔCCT`],  [B 색온도 \u{2212} A 색온도 (K). 부호 포함],
  [`ΔE76`],  [CIE 1976 `L*a*b*` 색차 (CIE76). 1.0 근방이 JND 목표],
)

#tip[
  *언제 여는가:* 보상 IP가 특정 영역의 색을 틀었는지 판정할 때.
  화이트 패치 위에 ROI를 씌우고 `ΔCCT` 와 `Δu'v'` 를 읽으면
  "밝기는 맞는데 색온도가 87K 낮아졌다" 같은 결론을 한 줄로 얻습니다.
  같은 ROI의 `ROI Diff |A-B|` 표에서 `Max Error` 를 함께 보면
  "평균은 맞는데 국소 최대 오차가 크다"는 패턴(= 노이즈 유입)도 걸러집니다.
  픽셀 한 점의 컬러메트리가 필요하면 #key("Shift+V") 프로브를 쓰십시오 (3장 참조).
]

=== ROI 해제와 창 닫기

#table(
  columns: (auto, 1fr),
  [*하고 싶은 것*], [*방법*],
  [ROI 모드 끄기 + ROI 지우기], [#key("Ctrl+E") 를 다시 누름],
  [ROI 통계 창 닫기],           [창의 `x` 버튼 또는 #key("Esc")],
  [다른 영역으로 바꾸기],       [ROI 모드를 켠 채 새로 드래그],
)

#warn[
  #key("Ctrl+E") 로 ROI 모드를 끄면 사각형과 통계는 사라지지만 창 자체는
  남아 `Ctrl+E 로 ROI 모드 ON → 패널 위에서 좌클릭 드래그` 라는 안내만 표시합니다.
  창까지 치우려면 `x` 버튼이나 #key("Esc") 를 쓰십시오. 반대로 `View` 메뉴의
  `ROI Stats` 항목은 ROI 모드와 창을 한 번에 켜고 끕니다.
]

== Scatter Plot \u{2014} #key("Ctrl+T")

=== 무엇이 보이는가

#key("Ctrl+T") 는 `Scatter Plot  (A vs B)` 창을 엽니다.
*두 이미지가 모두 로드되어 있을 때만* 그려집니다.
같은 좌표의 픽셀 한 쌍을 점 하나로 찍되, 가로축(X)은 A의 값,
세로축(Y)은 B의 값입니다. 두 축 모두 `0.00`\~`1.00` 로 정규화되어 있고
`0.25` 간격의 눈금이 붙습니다 (8비트는 값/255).

=== 대각선의 의미

차트에는 왼쪽 아래에서 오른쪽 위로 향하는 회색 대각선이 그려져 있습니다.
이것이 `B = A`, 즉 *완전 일치선*입니다.

#table(
  columns: (auto, 1fr),
  [*점 구름의 모습*], [*해석*],
  [대각선 위에 가늘게 겹침], [보상이 항등에 가까움 \u{2014} 사실상 무변경],
  [대각선보다 기울기가 큼/작음], [게인(gain) 편차. 밝은 쪽으로 갈수록 오차가 벌어짐],
  [대각선과 평행하게 위/아래로 밀림], [오프셋(offset) 편차 \u{2014} DC 바이어스],
  [대각선 주위로 넓게 퍼짐], [노이즈 또는 디더링. 퍼짐 폭이 곧 잡음 크기],
  [S자로 휨], [감마·톤커브 차이 \u{2014} LUT 곡선이 원본과 다름],
  [특정 구간에서 수평으로 눕는 계단], [클리핑 또는 코드 뭉개짐 \u{2014} 여러 입력이 한 출력으로],
  [세로 방향 여러 갈래로 갈라짐], [입력이 같은데 출력이 다름 = 공간 의존 보상(무라 보상 등)],
)

=== 채널 선택과 샘플링

창 상단에 `R` / `G` / `B` 라디오 버튼이 있어 한 번에 한 채널만 봅니다
(기본값 `R`). 오른쪽에 `|  X=A, Y=B` 라는 안내가 붙습니다.
점 색은 선택 채널을 따라 빨강/초록/파랑입니다.

성능을 위해 전체 픽셀을 다 찍지 않고 균등 간격으로 최대 *20,000 점*까지
추출합니다. 실제 개수는 창에 그대로 표시됩니다.

```
Total: 8,294,400 px  /  Sampled: 20,000
```

#table(
  columns: (auto, 1fr),
  [*항목*], [*값*],
  [최대 샘플 수], [20,000 (고정)],
  [샘플링 방식],  [선형 인덱스 균등 간격 추출 (stride)],
  [점 반지름],    [샘플이 5,000개 초과면 1.0 px, 이하면 1.5 px],
  [캐시 갱신],    [이미지 크기 또는 내용이 바뀔 때 자동 재추출 (시퀀스 이동·회전 포함)],
)

#warn[
  샘플링은 무작위가 아니라 균등 간격이므로, 이미지 가로폭의 약수와 같은
  주기를 갖는 패턴(예: 폭 3840에 stride 415)에서는 특정 위상만 뽑힐 수
  있습니다. 주기성이 강한 테스트 패턴을 볼 때는 ROI를 좁혀 잡고
  `ROI Diff |A-B|` 통계로 교차 확인하십시오.
]

#ex("게인 · 오프셋 편차 분리")[
  ```
  av ramp_orig.png ramp_comp_ip.png
  ```
  0\~255 램프 패턴을 열고 #key("Ctrl+T") 를 누릅니다. `R` 채널의 점 구름이
  대각선보다 살짝 왼쪽 위로 평행 이동해 있다면 R 출력에 일정한 플러스
  오프셋이 실린 것이고, 원점 근처는 붙어 있는데 오른쪽 끝에서 벌어졌다면
  게인이 1보다 큰 것입니다. `G` / `B` 로 전환해 세 채널의 벌어짐이 다르면
  화이트 밸런스가 계조에 따라 흔들린다는 뜻이므로, 그 지점에 ROI를 씌워
  #key("Ctrl+E") 의 `ΔCCT` 로 색온도 이동량까지 정량화하십시오.
]

#tip[
  *언제 여는가:* "보상 IP의 전달 함수(transfer curve)가 설계값과 같은가"를
  한 장으로 증명해야 할 때. 산점도 한 장이면 게인·오프셋·감마·클리핑을
  동시에 보여 줄 수 있어 리뷰 자료로 효율이 좋습니다.
]

== 차트와 통계 내보내기 \u{2014} #key("Shift+Ctrl+S")

=== 저장 창은 상황에 따라 달라진다

#key("Shift+Ctrl+S") 는 "지금 열려 있는 창"에 맞는 저장 대화상자를 엽니다.

#table(
  columns: (auto, 1fr),
  [*지금 열려 있는 창*], [*열리는 저장 창*],
  [`Histogram`],        [`Save Histogram`],
  [`H-Line Cut`],       [`Save H-Line Cut`],
  [`V-Line Cut`],       [`Save V-Line Cut`],
  [`Image Statistics`], [`Save Statistics`],
  [(분석 창 없음)],      [이미지 저장 창 (7장 참조)],
)

#warn[
  `Scatter Plot` 과 `ROI Statistics` 에는 전용 내보내기가 없습니다.
  이 둘만 열어 둔 채 #key("Shift+Ctrl+S") 를 누르면 차트 저장 창이 아니라
  일반 이미지 저장 창(`Save Images`)이 열립니다 (7장 참조).
  산점도와 ROI 수치는 화면에서 읽어 옮기거나, 8장의 헤드리스
  `--metrics` / `--probe` 로 대체하십시오.
]

=== 차트 저장 창 (`Save Histogram` / `Save H-Line Cut` / `Save V-Line Cut`)

창 위쪽에 두 개의 전역 옵션이 있습니다.

#table(
  columns: (auto, auto, 1fr),
  [*옵션*], [*기본값*], [*설명*],
  [`Resolution`], [`1920` x `1080`], [출력 PNG 크기(px). 최소 `320` x `240` 으로 자동 보정],
  [`Channels`],   [`Combined`],      [`Combined` = R/G/B를 한 장에, `Separate R/G/B` = 채널별 3장],
)

그 아래에는 `Image A` / `Image B` / `Diff` 세 항목이 있고,
각 항목마다 PNG 경로와 CSV 경로를 따로 편집할 수 있는 입력칸과 개별
`Save` 버튼이 있습니다. 기본 경로는 원본 이미지가 있던 디렉터리에
다음 규칙으로 자동 생성됩니다.

#table(
  columns: (auto, 1fr),
  [*항목*], [*기본 파일명 규칙*],
  [Histogram],   [`<stem>_histogram.png` / `.csv`],
  [H-Line Cut],  [`<stem>_hcut.png` / `.csv`],
  [V-Line Cut],  [`<stem>_vcut.png` / `.csv`],
  [Diff],        [`<stemA>_vs_<stemB>_<suffix>_diff.png` / `.csv`],
)

맨 아래 `Save All Checked` 버튼은 체크된 항목의 PNG와 CSV를 한꺼번에 씁니다.
성공하면 초록색으로 `Saved: <파일명>`, 실패하면 빨간색으로
`Error: Failed to write ...` 가 표시됩니다.

#note[
  `Separate R/G/B` 를 고르면 지정한 경로의 확장자 앞에 `_R` / `_G` / `_B` 가
  붙어 3개 파일이 생성됩니다. 예를 들어 `orig_histogram.png` 를 지정하면
  `orig_histogram_R.png`, `orig_histogram_G.png`, `orig_histogram_B.png` 가
  저장됩니다. CSV는 채널 분리와 무관하게 항상 한 파일에 R/G/B 열이 모두 들어갑니다.
]

#warn[
  절단면 저장은 *저장 버튼을 누른 시점의 pan 위치*로 행·열을 다시 계산합니다.
  A와 B 항목은 각자 자기 패널의 pan 을, `Diff` 항목은 A 패널의 pan 을 씁니다.
  또 화면의 `Diff` 섹션과 달리 저장 창의 `Diff` 항목은 Diff 모드가 꺼져 있어도
  두 이미지만 로드되어 있으면 활성화됩니다.
]

=== CSV 파일 형식

#table(
  columns: (auto, auto, 1fr),
  [*종류*], [*헤더*], [*내용*],
  [히스토그램], [`bin,R,G,B`],            [256행. 각 빈의 픽셀 개수],
  [절단면],     [`position,R,G,B`],       [선 길이만큼의 행. 정규화를 되돌린 실제 값 (8비트는 정수, float는 실수)],
  [통계],       [`Metric,R,G,B,Description`], [지표당 한 행 + 설명 열],
)

통계 CSV의 행 이름은 화면 표기와 조금 다릅니다 (공백 없는 식별자):
`Count`, `Min`, `Max`, `DynamicRange`, `Mean`, `Variance`, `StdDev`,
`Median`, `Skewness`, `Kurtosis`, `Energy`, `RMS`, `Entropy`.
`Diff` 항목에는 여기에 `MSE`, `PSNR`, `MAE`, `MaxError` 4행이 더 붙습니다.
숫자는 `%.6g` 형식(유효숫자 6자리)으로 기록됩니다.

```
Metric,R,G,B,Description
Count,8294400,8294400,8294400,Pixel count
Min,0,0,0,Minimum value
Max,255,255,255,Maximum value
DynamicRange,255,255,255,Max - Min
Mean,127.412,128.006,126.883,Arithmetic mean
...
PSNR,48.2214,47.9031,48.5507,Peak signal-to-noise ratio (dB)
MaxError,7,6,8,Maximum absolute error
```

#ex("리뷰 자료용 차트 일괄 저장")[
  ```
  av orig.png comp_ip_out.png --diff-mode abs
  ```
  + #key("Ctrl+H") 로 히스토그램을 엽니다.
  + #key("Shift+Ctrl+S") \u{2192} `Save Histogram` 창이 뜹니다.
  + `Resolution` 을 `2560` x `1440` 으로 올리고 `Separate R/G/B` 를 선택합니다.
  + `Image A` / `Image B` / `Diff` 세 항목을 모두 체크하고 `Save All Checked`.
  + 원본 디렉터리에 채널별 PNG 9장과 CSV 3개가 생성됩니다.
  CSV는 그대로 스프레드시트나 스크립트로 넘겨 회차별 추세를 그릴 수 있고,
  PNG는 보고서에 바로 붙일 수 있습니다. 같은 데이터를 GUI 없이 뽑아
  CI에 물리려면 8장의 `--metrics` / `--format` 을 사용하십시오.
]

#tip[
  라인컷 CSV의 `position,R,G,B` 는 밴딩 주기를 FFT로 분석하기에 딱 좋은
  형태입니다. 무라 보상 검증에서 "주기 8픽셀 성분이 보상 후 몇 dB 줄었는가"를
  정량화하려면, 보상 전후 두 CSV를 같은 행(row)에서 뽑아 비교하십시오.
]
