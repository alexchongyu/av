#import "_common.typ": *

= 색 관리와 룩(LUT · CDL)

DDI 보상 IP 를 검증할 때 화면에서 보이는 색과 파일에 들어 있는 숫자는 반드시
구분해야 합니다. "보상 후 영상이 좀 누렇게 보인다"는 말은 파일의 픽셀 값이
바뀌었다는 뜻일 수도 있고, 보고 있는 뷰어가 색을 변환해 보여 준다는 뜻일 수도
있습니다. 이 장에서는 av 가 파일의 값을 화면에 올리기까지 거치는 단계, 그 중
어디에 ICC 프로파일과 룩(LUT/CDL)이 끼어드는지, 그리고 *룩이 지표 계산에는 전혀
영향을 주지 않는다*는 av 의 핵심 설계 원칙을 소스 수준에서 확인합니다. 마지막으로
CIE 컬러메트리 계산을 담당하는 공용 색 코어를 다룹니다.

== 표시 파이프라인 \u{2014} 어느 단계에서 무엇이 적용되는가

av 의 화면 표시는 다음 순서로 진행됩니다. 파일 디코드 \u{2192} 색 관리 \u{2192}
룩(LUT/CDL) \u{2192} 채널 선택 \u{2192} 화면 표시. 이 순서는 `src/ui/main_window.cpp`
의 텍스처 업로드, `src/gl_texture.cpp` 의 내부 포맷, `src/shader_sources.h` 의
`IMAGE_FRAG_SRC` 프래그먼트 셰이더에서 그대로 확인할 수 있습니다.

#fig_color_pipeline()

#table(
  columns: (auto, auto, 1fr),
  [*단계*], [*담당 코드*], [*하는 일*],
  [디코드], [`src/image_loader.cpp`], [PNG/JPG/BMP/TGA/PGM/PPM/HDR 을 CPU 버퍼로. LDR 은 8bit RGBA, HDR 은 RGBA32F, PPM 은 원본 16bit 값을 별도 보관],
  [텍스처 업로드], [`src/gl_texture.cpp`], [LDR 은 `GL_RGBA8`/`GL_RGB8`/`GL_R8`, HDR 은 `GL_RGBA32F`. *sRGB 내부 포맷을 쓰지 않으므로 업로드 단계에서 값이 변형되지 않습니다*],
  [색 관리], [`--profile` / `--no-color-mgmt`], [옵션으로 의도를 기록. 현재 릴리스에서는 변환이 적용되지 않습니다 (아래 절 참조)],
  [룩(LUT/CDL)], [`src/lut.cpp` + 셰이더 `u_lut`], [`--lut`/`--cdl` 로 로드한 격자를 3D 텍스처로 샘플링. `'` 키로 ON/OFF],
  [채널 선택], [셰이더 `u_channel`], [`Shift+R/G/B` 단일 채널, `Shift+Y` Luma, `Shift+C` 복귀 (3장 참조)],
  [픽셀 그리드], [셰이더], [줌 16배 이상에서 격자선을 0.4 비율로 섞어 표시],
)

셰이더 본문의 실제 순서는 다음과 같습니다. 룩이 채널 선택보다 *먼저* 적용된다는
점이 중요합니다. 즉 `Shift+R` 로 R 채널만 보고 있을 때 화면에 뜨는 밝기는
"LUT 를 통과한 뒤의 R" 입니다.

```
out_color = texture(u_tex, uv);                       // 1. 원본 샘플
if (u_lut_enabled == 1)                               // 2. 룩
    out_color.rgb = texture(u_lut, clamp(out_color.rgb, 0.0, 1.0)).rgb;
if (u_channel == 1) out_color = vec4(vec3(out_color.r), out_color.a);   // 3. 채널
// ... u_channel 4 = Luma = dot(rgb, vec3(0.2126, 0.7152, 0.0722))
if (u_zoom >= 16.0) { ... }                           // 4. 픽셀 그리드
```

#warn[
`--software` 로 실행한 소프트웨어 렌더러는 CPU 경로(`cpu_render_image()` 뒤에
`lut_apply()`)를 쓰기 때문에 *채널 선택이 룩보다 먼저* 적용됩니다. 채널 필터와
룩을 동시에 켠 상태에서 GPU 모드와 `--software` 모드의 화면이 다르게 보일 수
있습니다. 룩을 정량적으로 비교할 때는 한쪽 렌더 경로로 통일하십시오.
]

#note[
보간 방식도 두 경로가 다릅니다. GPU 는 `GL_TEXTURE_3D` 의 `GL_LINEAR` 하드웨어
삼선형 보간, 소프트웨어 렌더러는 `lut_apply()` 의 CPU 삼선형 보간을 씁니다.
결과는 대체로 같지만 격자 사이 값에서 1\~2 LSB 차이가 날 수 있습니다.
]

== 룩은 지표와 픽셀 판독에 영향을 주지 않는다

이 절이 이 장에서 가장 중요합니다. av 의 룩은 *표시 전용(display transform)* 입니다.
LUT 나 CDL 을 걸어 둔 채로 PSNR 을 재도, 픽셀 값을 읽어도, 컬러메트리를 프로브해도,
파일에 저장해도 결과는 룩을 걸지 않았을 때와 완전히 동일합니다.

#table(
  columns: (auto, auto, 1fr),
  [*경로*], [*룩 적용*], [*근거*],
  [패널 A / B 화면], [적용], [`render_single()` \u{2192} 이미지 셰이더 `u_lut_enabled`],
  [`--comp` 세 패널], [적용], [세 패널 모두 `render_single()` 을 통과 (5장 참조)],
  [Diff 패널], [미적용], [Diff 는 `DIFF_FRAG_SRC` 를 쓰며 LUT 유니폼 자체가 없음 (4장 참조)],
  [SSIM / FLIP 히트맵], [미적용], [`lut_on` 계산에서 두 모드를 명시적으로 제외],
  [#key("V") 픽셀 값 풍선], [미적용], [`ImageEntry` 의 원본 픽셀 배열을 직접 읽음],
  [#key("Shift+V") 컬러메트리], [미적용], [같은 원본 배열에서 `color::srgb_to_xyz()` 호출],
  [히스토그램 · 라인컷 · 통계], [미적용], [모두 CPU 픽셀 배열 기반 (6장 참조)],
  [#key("Ctrl+S") 저장 / 클립보드], [미적용], [`src/image_save.cpp`, `src/clipboard_image.cpp` 에 LUT 참조 없음],
  [`--metrics` `--probe` `--uniformity` `--comp-out` `--diff-out` `--validate` `--batch`], [미적용], [`src/metrics_cli.cpp` 전체에 LUT 참조가 없음],
)

헤드리스에서는 아예 파일조차 읽지 않습니다. `src/main.cpp` 는 헤드리스 모드를
먼저 디스패치하고 나서(그리고 대부분 그 자리에서 종료) `AppState` 를 만들며,
`.cube`/`.cdl` 파싱은 그보다 뒤인 `apply_cli_options()` 안에서 일어나기 때문입니다.

#ex("룩을 걸어도 PSNR 은 그대로")[
같은 두 영상에 대해 룩 유무만 바꿔 지표를 뽑아 보면 CSV 가 바이트 단위로
동일합니다. CI 게이트(8장 참조)를 쓰는 팀은 이 성질 덕분에 "검토용 룩" 을
스크립트에 남겨 둬도 안전합니다.

```
av --metrics orig.png comp.png > a.csv
av --metrics orig.png comp.png --cdl shadow_lift.cdl > b.csv
diff a.csv b.csv        # 차이 없음 (--cdl 은 헤드리스에서 파싱조차 되지 않는다)
```
]

#tip[
반대로 말하면 "룩을 적용한 결과물" 을 지표로 평가할 수는 없습니다. 보상 IP 의
출력에 감마 룩을 씌운 상태를 정량 평가해야 한다면, 룩을 미리 구워 넣은 PNG 를
따로 만들어 그 파일을 `--metrics` 에 넣어야 합니다.
]

== ICC 프로파일과 색 관리 스위치

=== `--profile <file>`

ICC 프로파일 파일 경로를 지정합니다. 기본값은 없음(빈 문자열)이며 `.av.ini` 에
저장되지 않으므로 실행할 때마다 지정해야 합니다. 값을 생략하면
`Expected value after --profile` 를 출력하고 종료 코드 `1` 로 끝납니다.

```
av --profile /usr/share/color/icc/sRGB.icc orig.png comp.png
```

=== `--no-color-mgmt`

색 관리를 끄고 파일의 값을 그대로 화면에 올리라는 스위치입니다. 기본값은 꺼짐
(`no_color_mgmt = false`)이고 역시 영속화되지 않습니다.

```
av --no-color-mgmt orig.png comp.png
```

=== 현재 지원 범위와 한계

정직하게 밝혀야 할 사실이 있습니다. 두 옵션은 `CliOptions::icc_profile` 과
`CliOptions::no_color_mgmt` 에 값을 *기록만* 하며, 현재 릴리스의 렌더 파이프라인은
이 값을 참조하지 않습니다. 빌드 시스템은 lcms2 를 선택적으로 찾아
`AV_HAS_LCMS2` 를 정의하지만(`CMakeLists.txt`), 소스 어디에서도 이 매크로를 쓰지
않습니다. 즉 *두 옵션의 유무와 관계없이 av 의 표시는 항상 `--no-color-mgmt` 상태*
입니다.

#table(
  columns: (auto, 1fr),
  [*항목*], [*현재 동작*],
  [`--profile <file>`], [경로만 저장. 프로파일 변환 미적용. 존재하지 않는 파일을 줘도 오류 없음],
  [`--no-color-mgmt`], [플래그만 저장. 이미 기본 동작이므로 화면 변화 없음],
  [텍스처 내부 포맷], [`GL_RGBA8` 등 선형 포맷 \u{2014} GPU 의 자동 sRGB 감마 변환 없음],
  [모니터 프로파일], [OS 에 위임. av 는 개입하지 않음],
  [영속화], [둘 다 `.av.ini` 에 저장되지 않음],
)

#note[
이것은 검증 도구로서는 오히려 안전한 기본값입니다. *픽셀 값 검증*이 목적이라면
뷰어가 색을 손대지 않는 편이 낫습니다. `av` 로 코드값 `128` 인 회색을 열고
#key("V") 로 읽으면 항상 `128` 이 나오며, 어떤 프로파일 환경에서 실행하든
같은 값이 나옵니다. DDI 보상 IP 의 출력 LUT 를 코드값 단위로 대조할 때는
이 성질에 의존하십시오.
]

#warn[
따라서 av 의 화면 색을 "절대적인 색" 으로 신뢰해서는 안 됩니다. 광색역 모니터에
연결하면 sRGB 이미지가 과포화되어 보입니다. 이는 av 가 아니라 OS/모니터
프로파일 문제이며, av 는 그 위에서 값을 그대로 흘려보낼 뿐입니다. 색을
"보는" 판단이 필요할 때는 sRGB 로 캘리브레이션된 모니터를 쓰고, 수치 판단은
반드시 #key("Shift+V") 프로브나 `--probe` 로 하십시오.
]

== 3D/1D LUT \u{2014} `--lut <file.cube>`

=== 무엇을, 왜

`.cube` 파일에 담긴 색 변환을 *양쪽 패널의 화면에* 적용합니다. 보상 IP 가
전제하는 감마 커브를 흉내 내 보거나, 어두운 영역의 차이를 눈으로 보기 위해
섀도우를 들어 올릴 때 씁니다. 로드에 성공하면 즉시 켜진 상태로 시작합니다
(`lut_enabled = true`).

```
av --lut looks/panel_gamma22.cube orig.png comp.png
```

로드에 실패하면 stderr 에 다음을 출력하고 *룩 없이 정상 실행을 계속*합니다.

```
[lut] failed to load .cube: looks/panel_gamma22.cube
```

=== `.cube` 파싱 규격

파서는 `src/lut.cpp` 의 `lut_load_cube()` 입니다. 규격은 아래 표가 전부입니다.

#table(
  columns: (auto, 1fr),
  [*요소*], [*처리*],
  [`LUT_3D_SIZE N`], [3D 격자. 데이터 개수가 정확히 `N*N*N*3` 이어야 하며 아니면 로드 실패],
  [`LUT_1D_SIZE N`], [1D 커브. 데이터 개수가 정확히 `N*3` 이어야 함. 채널별 커브를 N×N×N 격자로 구워 넣음],
  [`TITLE`], [무시],
  [`DOMAIN_MIN` / `DOMAIN_MAX`], [*무시* \u{2014} 입력 도메인은 언제나 `[0,1]` 로 가정],
  [`\#` 로 시작하는 줄], [주석으로 건너뜀],
  [빈 줄 / 공백 줄], [건너뜀],
  [데이터 줄], [공백으로 구분된 float 3개(R G B). 3개 미만이면 그 줄만 조용히 무시],
  [데이터 순서], [R 이 가장 빠르게 변하는 표준 순서. 내부 인덱스는 `((b*N + g)*N + r)*3`],
  [격자 크기 상한], [코드상 없음. GPU 3D 텍스처 한계(`GL_MAX_3D_TEXTURE_SIZE`, 최소 보장 256)와 메모리가 실질 한계],
  [실무 권장], [17 / 33 / 65 \u{2014} 업계 표준 크기],
)

#warn[
`DOMAIN_MIN`/`DOMAIN_MAX` 를 무시한다는 점을 잊지 마십시오. 10bit 코드값
도메인(`DOMAIN_MAX 1023 1023 1023`)으로 작성된 `.cube` 를 그대로 넣으면 값이
전혀 다르게 해석됩니다. av 에 넣기 전에 도메인을 `0.0 \~ 1.0` 으로 정규화해
다시 뽑으십시오.
]

=== 보간과 적용 범위

입력 RGB 는 `[0,1]` 로 클램프된 뒤 삼선형(trilinear) 보간으로 샘플링됩니다.
GPU 모드에서는 `GL_RGB32F` 3D 텍스처 + `GL_LINEAR` + `GL_CLAMP_TO_EDGE`, 소프트
웨어 모드에서는 `lut_apply()` 가 8개 이웃 격자점을 직접 섞습니다. 텍스처 업로드는
첫 프레임에 지연 수행되므로(`main_window.cpp`), 큰 LUT 를 걸면 첫 프레임만
살짝 느릴 수 있습니다.

=== 1D LUT 의 함정 \u{2014} 메모리

`LUT_1D_SIZE N` 은 채널별 1D 커브지만, av 는 이를 *N×N×N 3D 격자로 구워 넣습니다*.
즉 1D 점 개수가 그대로 3D 한 변의 길이가 됩니다. 메모리는 `N*N*N*3*4` 바이트로
폭증합니다.

#table(
  columns: (auto, auto, 1fr),
  [*N*], [*메모리*], [*판정*],
  [33], [약 0.4 MB], [권장],
  [65], [약 3.1 MB], [권장],
  [256], [약 192 MB], [비권장],
  [1024], [약 12 GB], [사실상 불가 \u{2014} 할당 실패/스왑],
)

#warn[
DaVinci Resolve 등에서 뽑은 1024점짜리 1D `.cube` 를 그대로 `--lut` 에 주면
12 GB 를 할당하려 시도합니다. 1D 커브는 64점 이하로 리샘플하거나, 아예 33점
3D `.cube` 로 변환해서 쓰십시오.
]

=== 룩 ON/OFF 토글 \u{2014} #key("'") 키

#key("'") (아포스트로피) 를 누르면 룩 적용이 토글됩니다. 조건은 두 가지입니다.
#key("Ctrl") / #key("Cmd") 가 눌려 있지 않아야 하고, `--lut` 또는 `--cdl` 로 로드된
격자가 실제로 있어야 합니다. 아무 룩도 로드하지 않은 상태에서 누르면 아무 일도
일어나지 않습니다.

#ex("보상 결과의 감마 편차를 눈으로 잡기")[
보상 IP 출력이 원본보다 어둡게 나오는 것 같은데 PSNR 은 45 dB 로 나쁘지 않은
상황입니다.

```
av --cdl looks/shadow_lift.cdl orig.png comp.png
```

1. 실행하면 두 패널 모두 섀도우가 들린 상태로 뜹니다. 저계조 차이가 눈에 띕니다.
2. #key("'") 를 눌러 룩을 끕니다 \u{2192} 원래 화면으로 즉시 돌아갑니다.
3. #key("'") 를 반복해 누르면서 "룩을 걸었을 때만 보이는 밴딩" 이 어느 패널에
   있는지 확인합니다.
4. 위치를 찾았으면 #key("V") 로 실제 코드값을 읽습니다. 이 값은 *룩과 무관한
   원본 값*이므로 그대로 리포트에 쓸 수 있습니다.
]

#warn[
룩이 켜져 있는지 알려 주는 상태바 표시나 오버레이는 없습니다. 화면이 이상해
보이는데 원인을 모르겠다면 먼저 #key("'") 를 눌러 보십시오. 스크린샷을 리포트에
붙일 때도 룩이 켜진 채 캡처하지 않았는지 반드시 확인해야 합니다.
]

== ASC-CDL \u{2014} `--cdl <file.cdl>`

=== 수식과 적용 순서

ASC-CDL 은 색보정 업계 표준인 SOP(Slope/Offset/Power) + SAT(Saturation) 파라미터
집합입니다. av 는 이를 읽어 33×33×33 격자로 구운 뒤 LUT 와 완전히 같은 경로로
적용합니다. 계산 순서는 다음과 같습니다.

```
# 1) SOP  — 채널별 (c = R, G, B)
v   = in[c] * slope[c] + offset[c]
v   = max(v, 0.0)                       # 음수는 0 으로 클램프 (pow 정의 보호)
out[c] = pow(v, power[c])

# 2) SAT  — Rec.709 루마 기준
luma   = 0.2126*out[R] + 0.7152*out[G] + 0.0722*out[B]
out[c] = clamp(luma + sat * (out[c] - luma), 0.0, 1.0)
```

순서가 핵심입니다. *채널별 SOP 가 먼저, 채도(SAT)가 나중*입니다. 채도를 먼저
적용하는 다른 툴과는 결과가 다르므로, 그레이딩 팀에서 받은 값을 그대로
넣었는데 색이 안 맞으면 이 순서를 먼저 의심하십시오.

=== 파싱 범위

파서는 `lut_load_cdl()` 이며 XML 을 완전히 파싱하지 않고 태그 이름으로
첫 번째 블록을 찾아 읽는 방식입니다.

#table(
  columns: (auto, 1fr),
  [*요소*], [*처리*],
  [확장자], [`.cdl` / `.ccc` 모두 동일하게 처리 (내용만 봄)],
  [`<Slope>`], [필수. 없으면 로드 실패. float 3개],
  [`<Offset>`], [선택. 기본 `0 0 0`],
  [`<Power>`], [선택. 기본 `1 1 1`],
  [`<Saturation>`], [선택. 기본 `1.0`],
  [노드 위치], [`<SOPNode>` / `<SATNode>` 안팎 무관 \u{2014} 파일 전체에서 태그를 찾음],
  [여러 개의 ColorCorrection], [*첫 번째 것만* 사용. `.ccc` 라이브러리의 두 번째 이후 항목은 무시],
  [네임스페이스 접두사], [미지원. `<cdl:Slope>` 형태는 인식하지 못함],
  [격자 크기], [33 고정 (CLI 로 변경 불가)],
)

`--lut` 과 `--cdl` 을 동시에 주면 *`--lut` 이 우선*하며 `--cdl` 은 조용히 무시
됩니다. 두 룩을 겹쳐 적용할 수는 없습니다.

#ex("보상 IP 의 채널 게인을 CDL 로 재현")[
드라이버 레지스터에 R 게인 `+5%`, B 게인 `-5%`, B 감마 `1.10`, 채도 `1.10` 이
설정돼 있다고 합시다. 이를 `ip_v3.cdl` 로 적으면 다음과 같습니다.

```
<ColorCorrection id="ip_v3">
  <SOPNode>
    <Slope>1.05 1.00 0.95</Slope>
    <Offset>0.00 0.00 0.01</Offset>
    <Power>1.00 1.00 1.10</Power>
  </SOPNode>
  <SATNode><Saturation>1.10</Saturation></SATNode>
</ColorCorrection>
```

```
av --cdl ip_v3.cdl orig.png comp.png
```

이 설정에서 입력 중간 회색 `(0.25, 0.25, 0.25)` 는
`(0.2637, 0.2500, 0.2118)`, 8bit 로는 `(67, 64, 54)` 로 변환됩니다. 흰색
`(1,1,1)` 은 `(255, 255, 242)` 가 되어 화이트 포인트가 노란 쪽으로 이동한 것을
바로 볼 수 있습니다.
]

=== 33×33×33 베이킹의 정밀도 한계

CDL 은 격자에 구워지므로 격자점 사이 값은 선형 보간됩니다. 커브가 강한
비선형이면 *첫 격자 구간(코드값 0 \~ 8)* 에서 오차가 커집니다. 예를 들어
`<Power>0.45 0.45 0.45</Power>` (감마 리프트) 를 적용했을 때 실제 값은 다음과
같습니다.

#table(
  columns: (auto, auto, auto, 1fr),
  [*입력 8bit*], [*수식대로면*], [*av 표시*], [*설명*],
  [1], [21], [7], [첫 격자 구간 안 \u{2014} 오차 14 LSB],
  [2], [29], [13], [첫 격자 구간 안 \u{2014} 오차 16 LSB],
  [4], [39], [27], [첫 격자 구간 안 \u{2014} 오차 12 LSB],
  [8], [54], [54], [`8/255` 가 첫 격자점 `1/32` 과 거의 일치 \u{2014} 정확],
  [16], [73], [73], [정확],
  [32], [100], [100], [정확],
  [128], [187], [187], [정확],
)

즉 격자점(`k/32`) 위에서는 정확하고, 0 과 첫 격자점 사이에서만 커브가 직선으로
잘려 나갑니다. 블랙 디테일을 이 룩으로 판정하면 안 되는 이유입니다.

#warn[
룩은 "눈으로 보기 위한 도구" 이지 정밀 감마 검증 도구가 아닙니다. 감마 특성을
숫자로 검증해야 한다면 룩을 쓰지 말고 `--probe` 의 `Lstar` 열이나 히스토그램·
라인컷(6장 참조)을 쓰십시오. 그래도 룩의 저계조 정확도가 필요하다면, CDL 대신
65점 이상의 `.cube` 를 직접 구워 `--lut` 으로 넣으면 격자가 촘촘해집니다.
]

== 색 코어 \u{2014} 컬러메트리 계산의 단일 소스

=== 변환 체인

`src/color.cpp` 는 GL/SDL/ImGui 에 전혀 의존하지 않는 순수 계산 모듈이며,
모든 계산이 double 정밀도입니다. 변환 체인은 다음과 같습니다.

#table(
  columns: (auto, 1fr),
  [*함수*], [*내용*],
  [`srgb_to_linear()`], [sRGB EOTF (IEC 61966-2-1). `c <= 0.04045` 이면 `c/12.92`, 아니면 `((c+0.055)/1.055)^2.4`],
  [`linear_to_srgb()`], [역변환. `c <= 0.0031308` 이면 `c*12.92`, 아니면 `1.055*c^(1/2.4) - 0.055`],
  [`srgb_to_xyz()`], [감마 인코딩된 sRGB 값 \u{2192} 선형화 \u{2192} XYZ (LDR 이미지용)],
  [`lin_rgb_to_xyz()`], [이미 선형인 값 \u{2192} XYZ (HDR float 이미지용)],
  [`xyz_to_xy()`], [CIE 1931 색도 `x = X/(X+Y+Z)`, `y = Y/(X+Y+Z)`],
  [`xyz_to_upvp()`], [CIE 1976 UCS `u' = 4X/(X+15Y+3Z)`, `v' = 9Y/(X+15Y+3Z)`],
  [`cct_mccamy()`], [McCamy 1992 근사. `n = (x-0.3320)/(y-0.1858)`, `CCT = -449*n^3 + 3525*n^2 - 6823.3*n + 5520.33` (단위 K)],
  [`duv_from_upvp()`], [u\u{2032}v\u{2032} \u{2192} 1960 UCS `(u, v = 2v'/3)` \u{2192} Ohno/Krystek 6차 다항식. 부호는 흑체궤적 위(+)/아래(\u{2212})],
  [`xyz_to_lab()`], [CIELab. 기준 백색 CIE 2\u{00B0} D65 = `(0.95047, 1.00000, 1.08883)`],
  [`delta_e76()`], [CIE76 색차 `sqrt(dL^2 + da^2 + db^2)` (L\*a\*b\* 각 성분 차의 제곱합 제곱근)],
)

sRGB \u{2192} XYZ 행렬은 Bradford 적응된 표준 행렬이며, Y 행이 곧 Rec.709 휘도
가중치입니다.

```
X = 0.4123907993 R + 0.3575843394 G + 0.1804807884 B
Y = 0.2126390059 R + 0.7151686788 G + 0.0721923154 B
Z = 0.0193308187 R + 0.1191947798 G + 0.9505321522 B
```

#note[
av 안에는 서로 다른 두 개의 "휘도" 개념이 공존하며 혼동하면 안 됩니다.
*CIE Y* (광도적 휘도)는 위 행렬의 Y 행을 *선형* RGB 에 적용한 값으로,
컬러메트리 프로브와 `--uniformity` 가 씁니다. 반면 *Rec.709 Y\u{2032}* (비디오 루마)는
`0.2126R' + 0.7152G' + 0.0722B'` 를 *감마 인코딩된* 값에 그대로 적용한 것으로,
diff/SSIM/luma-PSNR 과 #key("Shift+Y") Luma 채널 뷰가 씁니다.
]

=== GUI 프로브와 헤드리스가 같은 코어를 쓴다

#key("Shift+V") 컬러메트리 풍선말(3장 참조), ROI 통계 창의 ROI 평균 색도(6장 참조),
헤드리스 `--probe`, `--uniformity` 는 *모두 같은 `color::` 함수를 호출*합니다.
따라서 GUI 에서 눈으로 읽은 값과 CI 스크립트가 뽑은 CSV 값이 어긋날 수 없습니다.

#table(
  columns: (auto, auto, 1fr),
  [*진입점*], [*표시*], [*소스*],
  [#key("Shift+V")], [`A xy 0.3127,0.3290  6505K` 행, `B` 행, 그리고 \u{0394}u\u{2032}v\u{2032} · \u{0394}CCT 행], [`main_window.cpp`],
  [ROI 통계 창], [ROI 평균의 xy · u\u{2032}v\u{2032} · CCT · Duv, 두 장이면 \u{0394}], [`chart_windows.cpp`],
  [`--probe <X,Y>`], [`which,X,Y,Z,x,y,uprime,vprime,CCT,Duv,Lstar` CSV], [`metrics_cli.cpp`],
  [`--uniformity`], [ICDM 균일도 + 색편차 `duv_max`], [`metrics_cli.cpp`],
)

입력 값 해석 규칙도 공통입니다. PPM 원본 값이 있으면 그 `maxval` 로 정규화한 뒤
sRGB 로, HDR float 이면 선형광으로, 그 외 LDR 8bit 이면 `/255` 후 sRGB 로 봅니다.

#ex("흰색 픽셀의 컬러메트리 검증")[
코드값 `(255,255,255)` 인 8bit PNG 와 `(128,128,128)` 인 PNG 를 프로브하면
다음이 나옵니다.

```
av --probe 4,4 white.png gray.png
```

```
which,X,Y,Z,x,y,uprime,vprime,CCT,Duv,Lstar
A,0.950456,1,1.08906,0.3127,0.329,0.19783,0.46832,6505.08,0.00320742,100
B,0.205166,0.215861,0.235085,0.3127,0.329,0.19783,0.46832,6505.08,0.00320742,53.585
delta,,,,,,,,0,2.77556e-17,46.415
```

흰색의 XYZ 가 D65 백색 `(0.95047, 1.0, 1.08883)` 과 일치하고, `x=0.3127`,
`y=0.3290`, CCT \u{2248} 6505 K 로 교과서 값이 그대로 나옵니다. 코드값 128 은
무채색이므로 색도가 동일하고 `Lstar` 만 `53.585` 로 떨어집니다. 이 값은
"코드값 50% = 밝기 50%" 가 아니라는 것을 보여 주는 좋은 근거 자료입니다.
]

#warn[
`Duv` 가 정확히 0 이 아니라 `0.0032` 로 나오는 것은 오류가 아닙니다. D65 자체가
흑체궤적에서 살짝 위에 있는 광원이기 때문입니다.

또한 검은 픽셀 `(0,0,0)` 처럼 `X+Y+Z = 0` 인 경우 `xy` 가 `(0,0)` 으로 폴백되며,
그 값을 McCamy 식에 넣으면 `CCT = 2021 K`, `Duv = 0.23` 같은 *무의미한 숫자*가
나옵니다. 매우 어두운 영역이나 클리핑된 영역의 CCT/Duv 는 판독하지 마십시오.
McCamy 근사 자체도 흑체궤적 근방에서만 유효합니다.
]

=== FLIP 은 별도의 D65 경로다

`src/flip_engine.cpp` 는 자체 D65 상수 `(0.950428545, 1.0, 1.088900371)` 을
가지고 있으며, 위의 색 코어를 쓰지 않습니다. 값이 미세하게 다른 것은 실수가
아니라 *의도된 분리*입니다. FLIP 은 NVIDIA 레퍼런스 구현과 수치가 일치해야 하는
검증된 코드이므로, 컬러메트리를 정리하면서 FLIP 을 건드려 회귀를 만들지 않도록
일부러 손대지 않았습니다.

#note[
정리하면 av 에는 두 개의 D65 가 있습니다. 컬러메트리(프로브 · ROI · 균일도)는
CIE 표준 D65 `(0.95047, 1.0, 1.08883)` 을 써서 발표된 레퍼런스와 값이 맞고,
FLIP 지표는 NVIDIA 레퍼런스와 값이 맞습니다. 두 값을 통일하려는 시도는
어느 한쪽의 검증을 깨뜨립니다.
]

== 실무 활용 \u{2014} DDI 검증 시나리오

=== 패널 측정 데이터와 대조하기

CA-410 이나 CS-2000 같은 색채휘도계로 패널을 실측하고, 그 데이터를 av 의 프로브
값과 대조하는 흐름입니다. 먼저 성격 차이를 이해해야 합니다.

#table(
  columns: (auto, 1fr),
  [*측정기 값*], [패널이 실제로 방출한 빛의 색도. 백라이트 · 컬러필터 · 셀 특성이 모두 반영됨],
  [av 프로브 값], [*파일의 코드값을 이상적인 sRGB 디스플레이가 표시한다고 가정*했을 때의 색도. 패널 특성은 반영되지 않음],
)

따라서 두 값을 직접 빼서 비교하면 안 됩니다. 실무에서는 다음 순서로 씁니다.

1. 테스트 패턴(예: R/G/B/W 풀필드, 그레이스케일 스텝)의 *입력 파일*을 `--probe`
   로 찍어 "의도한 색도" 를 확보합니다.
2. 같은 패턴을 패널에 띄워 측정기로 "실제 색도" 를 잽니다.
3. 두 색도의 차이(\u{0394}u\u{2032}v\u{2032}, \u{0394}CCT)가 패널+IP 가 만들어 낸 오차입니다.
4. 보상 IP 를 태운 출력 파일을 다시 `--probe` 로 찍어, IP 가 의도한 방향으로
   보정했는지 확인합니다.

```
# 원본과 보상 결과의 같은 좌표를 한 번에 비교 (delta 행이 자동 출력)
av --probe 960,540 pattern/W100.png comp/W100.png
```

`delta` 행의 `CCT` 열은 \u{0394}CCT, `Duv` 열은 \u{0394}u\u{2032}v\u{2032} 거리, `Lstar` 열은
\u{0394}E76 입니다 (8장 참조). 풀필드 패턴이라면 한 점 대신 `--uniformity` 로
9/13/25점 균일도와 `duv_max` 를 한 번에 뽑는 편이 낫습니다.

#tip[
측정 데이터로부터 "이 패널에서 이렇게 보인다" 를 *미리 보고* 싶다면, 측정
결과로 3D LUT 을 만들어 `--lut` 으로 걸면 됩니다. 이렇게 하면 av 화면이 패널
시뮬레이터가 됩니다. 다만 이 상태의 화면은 어디까지나 예측이며, 지표와 프로브
값은 여전히 원본 파일 기준이라는 점을 잊지 마십시오.
]

=== LUT 으로 감마 보정 후 비교하기

보상 IP 의 결함은 대부분 저계조(블랙 디테일, 밴딩, 디더 패턴)에서 먼저
드러나는데, 감마 2.2 로 인코딩된 이미지를 그대로 보면 이 영역이 너무 어두워
눈에 띄지 않습니다. 이때 섀도우를 들어 올리는 룩을 임시로 걸어 두고 비교하면
검출률이 크게 올라갑니다.

```
# shadow_lift.cdl: power 0.45 (감마 리프트), slope/offset 은 항등
av --cdl shadow_lift.cdl orig.png comp.png
```

권장 작업 순서입니다.

1. `--cdl` 또는 `--lut` 으로 룩을 걸고 실행합니다.
2. #key("'") 로 룩을 껐다 켜며 "룩을 켰을 때만 보이는 차이" 를 찾습니다.
3. 의심 영역을 찾으면 Diff 모드(4장 참조)로 전환합니다. Diff 패널에는 룩이
   적용되지 않으므로 원본 차이가 그대로 보입니다.
4. #key("V") 와 #key("Shift+V") 로 원본 코드값과 색도를 읽어 기록합니다.
5. 최종 판정은 `--metrics` / `--comp-out` 수치로 합니다. 룩과 무관합니다.

#ex("세 영상 비교에 룩을 곁들이기")[
원본 · FW 보상 · HW 보상 세 장을 블록 비교하면서 저계조를 들여다보는 조합입니다.

```
av --comp orig.png fw.png hw.png --blk 16x16 --num_blk 24 --cdl shadow_lift.cdl
```

세 패널 모두에 같은 룩이 걸리므로 비교 조건이 동일합니다. 워스트 블록 랭킹과
PSNR 은 룩과 무관하게 원본 픽셀로 계산되므로, "눈으로 본 인상" 과 "숫자" 를
같은 화면에서 동시에 확인할 수 있습니다 (5장 참조).
]

== 요약과 주의점

#table(
  columns: (auto, auto, 1fr),
  [*옵션 / 키*], [*기본값*], [*요점*],
  [`--profile <file>`], [없음], [경로만 저장. 현재 변환 미적용. 영속화 안 됨],
  [`--no-color-mgmt`], [꺼짐], [플래그만 저장. av 는 원래 원시 값을 그대로 표시],
  [`--lut <file.cube>`], [없음], [3D/1D `.cube`. 로드 성공 시 즉시 ON. 도메인은 `[0,1]` 가정],
  [`--cdl <file.cdl>`], [없음], [ASC-CDL SOP+SAT 를 33 격자로 베이킹. `--lut` 이 있으면 무시됨],
  [#key("'")], [\u{2014}], [룩 ON/OFF. 로드된 룩이 없으면 무동작. 세션 한정, `.av.ini` 미저장],
  [#key("Shift+V")], [꺼짐], [컬러메트리 풍선말. `--probe` 와 같은 코어 (3장 참조)],
)

#warn[
이 장에서 반드시 기억할 세 가지입니다.

1. *룩은 화면에만 적용됩니다.* 지표(PSNR/SSIM/FLIP), 픽셀 값 판독, 컬러메트리,
   파일 저장, 클립보드 복사, 모든 헤드리스 출력은 룩의 영향을 받지 않습니다.
2. *룩이 켜졌다는 표시가 화면에 없습니다.* 스크린샷을 리포트에 넣기 전에
   #key("'") 로 상태를 확인하십시오.
3. *`--profile` 은 아직 색을 바꾸지 않습니다.* av 의 화면 색을 절대 기준으로
   삼지 말고, 수치 판정은 #key("Shift+V") 와 `--probe` 로 하십시오.
]
